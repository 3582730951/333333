# Non-secret official Codex profile for the isolated local matrix mock.
model = "gpt-5.6-sol"
model_provider = "poolserver"
model_reasoning_effort = "low"
approval_policy = "never"
sandbox_mode = "danger-full-access"

[model_providers.poolserver]
name = "Pool Server Local Matrix"
base_url = "http://127.0.0.1:18787/v1"
wire_api = "responses"
supports_websockets = true
experimental_bearer_token = "e2e-non-secret-placeholder"

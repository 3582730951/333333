...\214\226\345\256\241\350\256\241-2026-07-29.md" | 301 +++++++++++++++++++++
 internal/api/admin_config.go                       |   3 +
 internal/api/audit_test.go                         |  13 +
 internal/api/diagnostic_jobs.go                    |   2 +-
 internal/api/diagnostics_export.go                 | 130 +++++++--
 internal/api/diagnostics_export_test.go            |  73 ++++-
 internal/api/middleware.go                         |   5 +
 internal/api/request_metrics.go                    | 264 ++++++++++++++++++
 internal/api/request_metrics_test.go               |  81 ++++++
 internal/api/server.go                             |  48 ++--
 internal/api/system.go                             |  24 +-
 web-spa/src/features/observability/api/exports.ts  |  10 +-
 web-spa/src/lib/browserDownload.js                 |  25 ++
 web-spa/tests/browser-download.test.ts             |  28 ++
 web-spa/tests/diagnostic-export.test.ts            |   2 +-
 15 files changed, 942 insertions(+), 67 deletions(-)

diff --git "a/docs/\351\241\271\347\233\256\345\205\250\346\240\210\344\274\230\345\214\226\345\256\241\350\256\241-2026-07-29.md" "b/docs/\351\241\271\347\233\256\345\205\250\346\240\210\344\274\230\345\214\226\345\256\241\350\256\241-2026-07-29.md"
new file mode 100644
index 0000000..07d1d53
--- /dev/null
+++ "b/docs/\351\241\271\347\233\256\345\205\250\346\240\210\344\274\230\345\214\226\345\256\241\350\256\241-2026-07-29.md"
@@ -0,0 +1,301 @@
+# 项目全栈优化审计与实测报告（2026-07-29）
+
+## 1. 结论摘要
+
+本轮以“时间复杂度优先、真实链路验证”为准则，覆盖请求接入、记录与用量、上下文隔离、调度、上游转发、前端下载、管理面和运维面。
+
+已落地并验证：
+
+1. **诊断包下载卡转圈**
+   - 追加型大表只导出最新 `20,000` 行，同时在 manifest 保留源表总行数、遗漏行数和截断策略。
+   - 前端下载改用 `Blob`，缩短前端总等待上限至 5 分钟，队列无进展阈值降至 15 秒。
+   - 浏览器对象 URL 延迟回收，避免 Firefox/Safari 在点击后同步回收导致下载被取消。
+   - 70 万历史记录场景从 **110.714 秒降至 5.246 秒**，加速 **21.10 倍**，耗时下降 **95.26%**。
+   - Chrome 真实 UI 点击 `/console/audit` 的“导出诊断包”后 **3.087 秒完成下载**，ZIP 签名正确，按钮恢复可用。
+2. **WebSocket 配额切换停顿**
+   - 对已有健康替代账号的映射会话，先执行持久上下文轮转，不再先同步等待配额重置探测。
+   - 原用例每次 10.12–10.15 秒超时，修复后连续 5 次均为 0.13–0.15 秒并成功恢复工具调用上下文。
+3. **运维可观测性**
+   - 新增鉴权的 `GET /admin/metrics` Prometheus 文本端点。
+   - 使用固定 6 类路由、6 类状态和固定延迟桶，不携带原始 path、模型、账号等高基数字段。
+   - 请求观测基准为 **18.23–18.41 ns/op、0 B/op、0 allocs/op**。
+4. **空态管理页**
+   - `/admin/audit` 空结果稳定返回 `[]`，真实浏览器不再显示“数据读取失败”。
+5. **最终回归**
+   - Go 全量、`go vet`、目标竞态、核心竞态、前端完整验证、真实 Chrome 下载、70 万行诊断、TLS/HTTP2 日常负载均在共享远端服务器的隔离目录内执行。
+
+## 2. 测试约束与硬件
+
+远端硬件：
+
+| 项目 | 实测 |
+|---|---:|
+| CPU | AMD EPYC 9654，96 核 / 192 线程 |
+| L3 | 384 MiB |
+| 内存 | 377 GiB，无 Swap |
+| 测试盘 | XFS，测试开始时约 60 GiB 可用 |
+| 共享机负载 | load average 约 86–88 |
+| 本轮限额 | 仅 CPU `190,191`，`GOMAXPROCS=2`，`nice -n 15`，`ionice -c3` |
+
+所有远端数据、工具链、浏览器、缓存、数据库和结果均位于：
+
+```text
+/root/autodl-tmp/codex-pool-regression-20260729
+```
+
+这保证本轮没有占用整机 CPU，也没有读写其他用户目录。
+
+### 硬件使用建议
+
+- **网关请求面**：GPU 对当前协议代理、JSON/SSE、调度和加密链路没有收益；优先高主频 CPU、稳定网络和低延迟 NVMe。
+- **单机 SQLite**：建议 4–8 个受限 vCPU 起步，`GOMAXPROCS` 对齐容器 CPU quota；请求体内存预算和响应体预算必须小于容器内存上限并保留 20%–30% 余量。
+- **高写入或多副本**：SQLite 单写者会先成为瓶颈；使用现有 PostgreSQL + active/standby + fencing 路径。
+- **大请求**：保留内存/磁盘分层 body source，spool 目录应放在独立 NVMe/XFS，并监控磁盘余量、文件数和写入延迟。
+- **共享/容器环境**：按 CPU quota 设置 `GOMAXPROCS`，按内存限制设置 `GOMEMLIMIT`；避免按宿主机 192 线程错误扩张工作队列。
+
+## 3. 架构与复杂度审计
+
+### 3.1 主链路
+
+```text
+HTTP/WS 接入
+  -> 鉴权、限流、请求体分层捕获
+  -> 用户组/模型/Provider 策略
+  -> CPA 会话映射与 Goal 连续性
+  -> 调度索引、亲和性、账号/出口租约
+  -> 上游 HTTP/SSE/WS 转发与重试
+  -> 流式过滤、用量提取、异步日志/Journal
+  -> 下游响应
+```
+
+### 3.2 热路径复杂度
+
+符号：`B` 请求字节数，`S` 响应字节数，`N` 候选账号数，`H` 可恢复历史大小，`R` 历史记录数，`L=20,000` 为单张诊断大表导出上限。
+
+| 阶段 | 稳态时间 | 额外空间 | 结论 |
+|---|---:|---:|---|
+| 请求体捕获 | `O(B)` | `O(min(B, memory_threshold))`，其余落盘 | 已有内存/磁盘双预算与磁盘保留保护 |
+| 路由候选索引构建 | `O(N)` | `O(N)` | 缓存构建，不放在每次采样热路径 |
+| 请求候选采样 | `O(1)` | `O(1)` | 10、1,000、10,000 账号均约 13–15 ns、0 alloc |
+| 亲和 Top-3 构建 | `O(N)` | `O(N)` | 缓存刷新阶段；10k 账号约 609 µs |
+| 等待队列取消 | `O(1)` | `O(1)` | 侵入式移除，约 448–461 ns |
+| CPA 别名查询 | 索引下 `O(log M)` | `O(1)`/结果集 | HMAC 命名空间，避免原始下游标识串号 |
+| 上下文正常续接 | `O(1)` 元数据 + `O(B)` 转发 | 有界 | 原生 upstream session 为主路径 |
+| 上下文故障重建 | `O(H+B)` | `O(H+B)` | 仅绑定丢失、上下文丢失或会话风险时触发 |
+| HTTP/SSE/WS 转发 | `O(S)` | 固定缓冲/有界 spool | SSE buffer 复用、WS 顺序会话 |
+| 用量提取 | `O(S)` | 有界 scanner | 请求后异步队列，批量最多 64 条/5 ms |
+| Journal 追加 | 摊销 `O(1)` | 有界队列 | 崩溃恢复仍与待重放段大小线性相关 |
+| 诊断大表行物化（修复前） | `O(R)` | archive 与扫描随 `R` 增长 | 直接导致前端长时间转圈 |
+| 诊断大表行物化（修复后） | `O(min(R,L))` | `O(min(R,L))` | 最新 20k 行，DLP/ZIP 体积被上限约束 |
+| 诊断 COUNT/聚合 | `O(R)`（数据库侧） | `O(1)` | **剩余 P1**：源行数、部分汇总仍扫描历史表 |
+| HTTP 指标观测 | `O(1)` | 固定数组 | 18.23–18.41 ns，0 alloc，标签基数固定 |
+
+### 3.3 仍需优先处理的复杂度项
+
+1. **P1：诊断 metadata 的全表聚合**
+   - 当前导出负载和 DLP 已受 `L` 限制，但精确 `COUNT(*)`、时间范围和部分审计汇总仍为 `O(R)`。
+   - 建议按小时维护增量 rollup，并把精确扫描改为管理员显式“深度诊断”模式。
+2. **P1：Journal 恢复游标**
+   - 正常追加为摊销 `O(1)`；启动恢复仍需扫描待重放段。
+   - 建议持久化已确认 segment/offset 游标，减少大故障恢复时间。
+3. **P2：超大文件拆分**
+   - `internal/storage/storage.go` 约 10,207 行，`internal/api/server.go` 约 3,600 行，诊断导出约 2,700 行。
+   - 建议按领域拆为 repository/service/transport，降低变更冲突和回归半径。
+
+### 3.4 五条性能链路逐项裁决
+
+| 链路 | 当前瓶颈或风险 | 本轮优化/现有有界设计 | 权威验证 | 下一决策 |
+|---|---|---|---|---|
+| 上游 | 映射 WebSocket 出现配额/会话风险时，先做 reset-credit 同步探测会阻塞整条流 | 有健康替代账号时先轮转并恢复持久上下文；无替代账号时仍保留原地 reset-credit | 同一回归修复前 5/5 在 10.12–10.15 秒失败，修复后 5/5 在 0.13–0.15 秒通过 | Provider Capability Registry、live balance 与按能力熔断 |
+| 记录 | 追加型 `usage_records`、`audit_log` 随历史无限增长，诊断物化和压缩原为 `O(R)` | 导出最新 `L=20,000` 行、流式 CSV、异步 usage queue、可恢复 Journal | 500k usage + 200k audit 从 110.714 秒降至 5.246 秒；Journal/core race 通过 | 小时 rollup、恢复 segment/offset 游标 |
+| 试用/使用/用量 | 大请求会放大 token 预留、账本写入和单机 SQLite 写竞争 | billing hold、异步 usage 批次（最多 64 条/5 ms）、内存/磁盘 body 分层与总预算 | 16 个 31,869–32,098 token fixture、10 RPS、80/80 成功；Journal backlog 最终为 0 | Provider 实时余额、试用额度策略与报表 rollup |
+| 上下文处理与隔离 | 故障重建为 `O(H+B)`，错误的恢复次序会放大等待 | HMAC 命名空间、账号/出口/设备绑定、原生 session 常态 `O(1)` 元数据续接，仅风险时重建 | WebSocket 工具上下文轮转测试、CPA 映射指标、目标 race 均通过 | 100k 映射规模、跨区恢复和故障注入 |
+| 下游转发 | SSE/WS 背压、对象 URL 过早回收、重复 archive 副本会增加尾延迟和内存 | 固定缓冲/有界 spool、Blob 单份下载、对象 URL 延迟回收、固定基数请求指标 | TLS/HTTP2 80/80，P95 11.883 ms；真实 Chrome ZIP 下载 3.087 秒；指标 0 alloc | Realtime 扩面、每协议 P95/P99 告警 |
+
+这里的“试用”按项目实际语义同时核对试用额度、日常使用和 usage 记录链路；没有只用单一 API 单测代替整条请求—转发—记录闭环。
+
+## 4. 五种角色审计
+
+### 4.1 架构师
+
+**优势**
+
+- 接入、调度、CPA 映射、Goal、用量、诊断、HA 已形成完整控制面和数据面。
+- 上下文隔离使用 HMAC 别名、加密身份和账号/出口绑定，故障时才走持久重建。
+- PostgreSQL active/standby、fencing 和逻辑快照路径具备生产扩展基础。
+
+**风险与决策**
+
+- `server.go`、`storage.go` 仍承担过多职责；下一阶段优先拆模块，而不是继续在同一文件叠加功能。
+- 诊断导出已从“数据库副本导出”演进为逻辑、脱敏、异步任务，但统计平面还需 rollup 才能做到完全有界。
+- API 协议面与管理产品面耦合较强，建议形成 Provider Capability Registry，作为路由、UI、模型探测和协议转换的统一事实源。
+
+### 4.2 算法工程师
+
+**已验证**
+
+- 调度请求采样为 `O(1)`；索引构建和亲和 Top-K 为可缓存的 `O(N)`。
+- body source 对大请求仅线性扫描一次，并在内存/磁盘间分层。
+- 新指标为真正固定空间与固定标签，不会随 URL/账号数增长。
+
+**下一步**
+
+- 对诊断聚合、用量报表和 Goal 检索建立增量 rollup/物化视图。
+- 把调度、body source、协议转换基准加入 CI 性能门槛，检测 ns/op、alloc/op 和 P95 回退。
+- 对 10k、100k 账号规模增加索引构建上限和缓存命中率仪表。
+
+### 4.3 普通用户
+
+**已改善**
+
+- 点击诊断导出后任务会被唤醒、轮询有明确时限，失败会显示真实原因。
+- 下载使用 Blob 并延迟回收 URL，避免浏览器下载被提前取消。
+- 实际 Chrome UI 下载 3.087 秒完成，按钮随后恢复。
+- 空审计库显示“暂无审计记录”，不再显示数据解析错误。
+
+**待补**
+
+- 下载按钮可进一步显示“排队 / 打包 / 校验 / 下载”四阶段进度和导出行数。
+- 建议在下载成功 Toast 中展示包大小、截断表数量和 manifest 入口。
+
+### 4.4 管理员
+
+**已改善**
+
+- manifest 明确显示源表行数、导出行数、遗漏行数、选择规则。
+- `/admin/metrics` 可直接接入 Prometheus；`/admin/system` 也含 HTTP 聚合快照。
+- 诊断任务由 30 分钟收敛为 5 分钟，前端队列卡住可更早告警。
+
+**待补**
+
+- `automation.go` 的计划策略仍缺少真正的定时执行器。
+- Provider live balance 检查仍有待实现。
+- 缺少 OIDC/社交登录和更细的管理员 RBAC。
+- 缺少面向管理员的协议能力矩阵与升级兼容报告。
+
+### 4.5 运维
+
+**已改善**
+
+- Prometheus 指标不暴露模型、账号、原始 URL 等高基数或敏感标签。
+- 32k-token、10 RPS、80 请求 TLS/HTTP2 场景 80/80 成功，P95 11.883 ms，峰值 RSS 39,116 KiB。
+- 诊断 70 万历史行场景峰值 RSS 42,408 KiB。
+
+**待补**
+
+- 新增默认告警规则：无健康账号、排队时长、诊断失败、Journal backlog、spool 磁盘、错误率、P95/P99、HA fencing。
+- 增加公开状态页、版本/迁移生命周期和升级前兼容检查。
+- 多副本生产环境建议以 PostgreSQL、独立对象存储诊断包和集中 Prometheus 为标准组合。
+
+## 5. 2026-07-20 之后仍活跃的 GitHub 项目对比
+
+严格按代码 push/commit 时间，本轮确认以下项目在 2026-07-20 之后仍有更新：
+
+- [QuantumNous/new-api](https://github.com/QuantumNous/new-api)（[GitHub API](https://api.github.com/repos/QuantumNous/new-api)）：2026-07-29 有 push。
+- [router-for-me/CLIProxyAPI](https://github.com/router-for-me/CLIProxyAPI)（[GitHub API](https://api.github.com/repos/router-for-me/CLIProxyAPI)）：2026-07-29 有 push。
+- [labring/aiproxy](https://github.com/labring/aiproxy)（[commit 页](https://github.com/labring/aiproxy/commits/main/)）：2026-07-21、24、27、29 均有提交，属于严格时间集合。
+
+| 能力 | 本项目 | new-api | CLIProxyAPI | aiproxy | 裁决 |
+|---|---|---|---|---|---|
+| 多账号调度/重试 | 强，含亲和、冷却、出口 | 有 | 有，轮询多账号 | 有，优先级/错误率路由 | 保持优势 |
+| 持久上下文隔离/故障重建 | **强项** | 未见同等 CPA/Goal 深度 | 会话 hash/账号轮询 | 多租户隔离 | 继续作为核心差异化 |
+| 内建用量/审计 | 强 | 强，含计费充值 | 核心已移除部分统计 | 强，含成本/渠道性能 | 保持 |
+| Prometheus/性能观测 | 本轮补齐固定基数请求指标 | 有 profiling/部署能力 | 推荐外置监控 | 有详细监控 | 继续补告警模板 |
+| Realtime/WS | Responses WS 强 | Realtime 覆盖更广 | 多协议 WS | 有协议转换 | 增加 OpenAI Realtime 面 |
+| Embeddings/图像/音频/视频/Rerank | **缺失** | 覆盖广 | 重点为 CLI 模型 | 覆盖音频/文档等 | P2 扩协议 |
+| OIDC/社交登录 | **缺失** | OIDC、Discord、Telegram 等 | 非核心 | 多租户管理 | P1 管理面 |
+| MCP 网关/插件 | 有零散 MCP 相关能力，未形成平台 | 非主要强项 | 非主要强项 | MCP/OpenAPI-to-MCP、插件系统 | P2 建平台 |
+| 自动化执行 | 有策略持久化，执行器待补 | 管理能力较完整 | 非核心 | 管理/插件丰富 | P1 完成 scheduler |
+| 主动告警/状态生命周期 | **缺失** | 部署生态较完整 | 推荐外部工具 | 有告警/请求日志 | P1 运维面 |
+
+### 本项目当前不可替代的优势
+
+1. CPA 原生续接、加密 Goal checkpoint、账户/出口/设备边界和故障轮转是一条完整链路。
+2. 诊断包具备匿名化、DLP、异步任务、逻辑快照、范围下载和截断 manifest。
+3. 对 Codex/Claude/Kiro 等真实客户端语义、缓存诊断、usage journal 和账号生命周期覆盖较深。
+
+### 应吸收的功能
+
+1. **P1**：OIDC/组织登录、主动告警、自动化执行器、Provider 实时余额。
+2. **P2**：Embeddings、Realtime、图像/音频、Rerank。
+3. **P2**：MCP/OpenAPI-to-MCP 网关与受控插件生命周期。
+4. **P2**：公开状态页、版本兼容矩阵、更新/迁移管理。
+
+## 6. 本轮代码变更
+
+| 路径 | 变更 |
+|---|---|
+| `internal/api/diagnostics_export.go` | 大表最新 20k 限制、源行数/截断 manifest、代码表查询同样有界 |
+| `internal/api/diagnostic_jobs.go` | 任务总时限 5 分钟 |
+| `internal/api/diagnostics_export_test.go` | 大表上限、新旧 ID 边界、race 可用等待窗 |
+| `web-spa/src/features/observability/api/exports.ts` | Blob 下载、5 分钟总时限、15 秒排队无进展检测 |
+| `web-spa/src/lib/browserDownload.js` | 对象 URL 延迟回收 |
+| `web-spa/tests/browser-download.test.ts` | 浏览器下载生命周期回归 |
+| `internal/api/request_metrics.go` | 固定基数、无分配 HTTP 指标与 Prometheus 编码 |
+| `internal/api/middleware.go` | 全局请求观测 |
+| `internal/api/system.go` | `/admin/system` HTTP 快照 |
+| `internal/api/server.go` | `/admin/metrics`；映射 WS 风险时优先轮转 |
+| `internal/api/admin_config.go` | 空审计返回 JSON 数组 |
+| `internal/api/audit_test.go` | 空审计契约测试 |
+| `internal/console/dist/` | 使用最终前端源码重建的嵌入控制台 |
+
+## 7. 实测矩阵
+
+| 验证 | 结果 |
+|---|---|
+| `go vet ./...` | 通过 |
+| `go test -p=1 ./... -count=1` | 通过 |
+| 诊断/指标/WS 目标 `-race` | 通过，98.667 秒 |
+| scheduler/body source/journal `-race` | 通过 |
+| 前端 `npm run verify` | 通过；47 个测试、类型检查、视觉 smoke、生产构建 |
+| Chrome UI 诊断下载 | 9,415 bytes，`504b0304`，3.087 秒，按钮恢复 |
+| 70 万行最终诊断 | 5.246 秒；500k usage + 200k audit；各导出最新 20k |
+| 70 万行修复前诊断 | 110.714 秒；导出全部 700k |
+| WebSocket 配额轮转 | 5/5 通过，0.13–0.15 秒 |
+| 日常流量模拟 | 80/80；10.117 RPS；P50 6.594 ms；P95 11.883 ms；P99 23.364 ms |
+| 日常负载资源 | RSS 39,116 KiB；15 FDs；8 threads；26 goroutines |
+| Prometheus smoke | 未鉴权 401；鉴权 200；6 个固定 route 标签 |
+
+## 8. 后续路线图
+
+### P1：下一迭代
+
+1. 诊断与报表增量 rollup，消除剩余全表聚合。
+2. Prometheus 告警规则和 Grafana dashboard。
+3. 自动化计划执行器、Provider live balance。
+4. OIDC、组织/管理员 RBAC。
+5. 将关键基准纳入 CI 性能门槛。
+
+### P2：产品扩展
+
+1. Embeddings、Realtime、图像/音频、Rerank。
+2. MCP/OpenAPI-to-MCP 与受控插件系统。
+3. 状态页、升级兼容矩阵、迁移控制面。
+4. 拆分 `server.go`、`storage.go`、诊断导出模块。
+
+### P3：规模化
+
+1. PostgreSQL 多副本标准部署模板。
+2. 诊断包对象存储与生命周期策略。
+3. 10k/100k 账号压力基线、故障注入和跨区恢复演练。
+
+## 9. 原始目标完成性矩阵
+
+| 原始要求 | 结论 | 直接证据 |
+|---|---|---|
+| 软件架构角度 | 已完成现状、边界、风险和分层决策 | §3.1、§4.1；主链路图及模块拆分决策 |
+| 算法工程师角度 | 已完成时间/空间复杂度和规模基准，时间优先 | §3.2、§4.2；调度 10/1k/10k、body source、指标 benchmark |
+| 使用者角度 | 已修复最直接的下载卡转圈和空态错误 | §1、§4.3；真实 Chrome 下载与前端完整验证 |
+| 管理员角度 | 已补 manifest 可解释性、指标和空审计契约 | §4.4、§6；鉴权 metrics smoke 和 API 契约测试 |
+| 运维角度 | 已补固定基数观测、硬件边界、资源与清理记录 | §2、§4.5；TLS/H2 日常负载、RSS/FD/thread/goroutine、远端清理 |
+| 硬件资源占用 | 已测 CPU、内存、磁盘并限制共享机资源 | §2；CPU `190,191`、`GOMAXPROCS=2`、nice/ionice、峰值资源 |
+| 上游性能 | 已修复阻塞式恢复次序 | §3.4；WebSocket 修复前/后 5 次对照 |
+| 记录性能 | 已把诊断大表物化从 `O(R)` 收敛至 `O(min(R,L))` | §3.2、§3.4；70 万行同输入前后对照 |
+| 试用/使用/用量 | 已核对额度、body、usage queue、Journal 闭环 | §3.4；32k-token 日常模拟、backlog=0、核心 race |
+| 上下文处理与隔离 | 已核对 CPA/Goal/HMAC 边界并修复故障轮转 | §3.1、§3.4；工具上下文恢复回归 |
+| 下游转发 | 已覆盖 HTTP/SSE/WS、TLS/H2 与浏览器下载 | §3.2、§3.4；80/80 与真实 ZIP 下载 |
+| GitHub 缺失功能分析 | 已用三个严格活跃项目对标并形成 P1/P2/P3 | §5、§8；仓库 API/commit 页及功能表 |
+| 完整测试和日常模拟 | 已执行全量、vet、race、前端、浏览器、性能和负载 | §7；原始日志与退出码见 `artifacts/optimization-20260729/verification-record.md` |
+| 可交付和回滚 | 已生成最终二进制、补丁、验证记录、哈希和可执行回滚 | `artifacts/optimization-20260729/`；正向应用和回滚均重新执行验证 |
+
+完成判定不把“待补”路线图伪装成本轮已实现项：本轮已经完成原始要求中的分析、优先级裁决、关键瓶颈优化和完整验证；§8 明确记录的是后续新增功能，而不是缺失的本轮测试证据。
diff --git a/internal/api/admin_config.go b/internal/api/admin_config.go
index 1641c0b..1c48b99 100644
--- a/internal/api/admin_config.go
+++ b/internal/api/admin_config.go
@@ -791,6 +791,9 @@ func (s *Server) adminAudit(w http.ResponseWriter, r *http.Request) {
 		writeError(w, http.StatusInternalServerError, err)
 		return
 	}
+	if rows == nil {
+		rows = []storage.AuditLogRow{}
+	}
 	writeJSON(w, http.StatusOK, rows)
 }
 
diff --git a/internal/api/audit_test.go b/internal/api/audit_test.go
index c2f4bff..b9ef41e 100644
--- a/internal/api/audit_test.go
+++ b/internal/api/audit_test.go
@@ -4,6 +4,7 @@ import (
 	"context"
 	"encoding/json"
 	"net/http"
+	"strings"
 	"testing"
 
 	"codex-account-pool/internal/storage"
@@ -42,3 +43,15 @@ func TestAdminAuditCanFilterByAccountID(t *testing.T) {
 		}
 	}
 }
+
+func TestAdminAuditEmptyReturnsJSONArray(t *testing.T) {
+	h := newHarness(t, func(w http.ResponseWriter, r *http.Request) {})
+
+	code, raw := grpReq(t, h, http.MethodGet, "/admin/audit?limit=10", "")
+	if code != http.StatusOK {
+		t.Fatalf("admin audit = %d: %s", code, raw)
+	}
+	if strings.TrimSpace(string(raw)) != "[]" {
+		t.Fatalf("empty audit response = %s, want []", raw)
+	}
+}
diff --git a/internal/api/diagnostic_jobs.go b/internal/api/diagnostic_jobs.go
index a9594ad..bcd689d 100644
--- a/internal/api/diagnostic_jobs.go
+++ b/internal/api/diagnostic_jobs.go
@@ -28,7 +28,7 @@ import (
 )
 
 const (
-	diagnosticJobTimeout           = 30 * time.Minute
+	diagnosticJobTimeout           = 5 * time.Minute
 	diagnosticArtifactTTL          = 24 * time.Hour
 	diagnosticJobPollInterval      = time.Second
 	diagnosticJobQueueCapacity     = 3 // one running + two queued
diff --git a/internal/api/diagnostics_export.go b/internal/api/diagnostics_export.go
index 768ca59..b3ed34a 100644
--- a/internal/api/diagnostics_export.go
+++ b/internal/api/diagnostics_export.go
@@ -192,11 +192,18 @@ func diagnosticFileOrder() []string {
 }
 
 type diagnosticExportStat struct {
-	Rows int64
-	Min  int64
-	Max  int64
+	Rows       int64
+	SourceRows int64
+	Min        int64
+	Max        int64
 }
 
+// A diagnostics bundle is a support snapshot, not a second full database
+// backup. Keeping the newest rows from each append-heavy table makes export
+// latency and memory/disk work predictable even after months of traffic. The
+// manifest records the exact source/exported counts whenever this cap applies.
+const diagnosticExportRowLimit int64 = 20_000
+
 func (s *Server) streamDiagnosticsExport(ctx context.Context, w http.ResponseWriter) error {
 	snapshot, err := s.store.BeginDiagnosticSnapshot(ctx)
 	if err != nil {
@@ -387,8 +394,19 @@ func (s *Server) writeDiagnosticsExport(ctx context.Context, dst io.Writer, snap
 	if err := addJSON("runtime_storage.json", s.runtimeStorageDiagnostics()); err != nil {
 		return err
 	}
+	sourceRowCounts := make(map[string]int64, len(stats))
+	truncatedTables := map[string]map[string]interface{}{}
 	for name, stat := range stats {
 		rowCounts[name] = stat.Rows
+		sourceRowCounts[name] = stat.SourceRows
+		if stat.SourceRows > stat.Rows {
+			truncatedTables[name] = map[string]interface{}{
+				"source_rows":   stat.SourceRows,
+				"exported_rows": stat.Rows,
+				"omitted_rows":  stat.SourceRows - stat.Rows,
+				"selection":     "most_recent",
+			}
+		}
 	}
 	rowCounts["manifest.json"] = 1
 	timeRanges := map[string]interface{}{}
@@ -512,9 +530,14 @@ func (s *Server) writeDiagnosticsExport(ctx context.Context, dst io.Writer, snap
 		"account_count": len(accounts), "current_account_count": len(accounts),
 		"historical_reference_account_count": len(codebook.byID),
 		"files":                              diagnosticFileOrder(), "row_counts": rowCounts,
-		"build": diagnosticBuildInfo(), "table_time_ranges": timeRanges,
-		"account_redaction":   "all entity identifiers use type-isolated stable HMAC aliases; no reverse map or alias key is included",
-		"account_code_format": "ACC-Base32(HMAC-SHA256(alias_key,domain||entity_type||raw_id)[:16])",
+		"source_row_counts":       sourceRowCounts,
+		"truncated_tables":        truncatedTables,
+		"large_table_row_limit":   diagnosticExportRowLimit,
+		"build":                   diagnosticBuildInfo(),
+		"table_time_ranges":       timeRanges,
+		"table_time_ranges_scope": "source_snapshot",
+		"account_redaction":       "all entity identifiers use type-isolated stable HMAC aliases; no reverse map or alias key is included",
+		"account_code_format":     "ACC-Base32(HMAC-SHA256(alias_key,domain||entity_type||raw_id)[:16])",
 	}
 	if err := addJSON("manifest.json", manifest); err != nil {
 		return err
@@ -600,14 +623,36 @@ func (s *Server) codexSessionMappingStatsFromSnapshot(ctx context.Context, snaps
 
 func buildStreamingDiagnosticCodebook(ctx context.Context, db storage.ReadQuerier, accounts []storage.Account, bindings []storage.AccountEgressBinding, aliasKey []byte) (diagnosticCodebook, error) {
 	rows, err := db.QueryContext(ctx, `
-SELECT account_id, MAX(account_label) FROM audit_log WHERE account_id <> '' GROUP BY account_id
-UNION SELECT account_id, '' FROM cf_events WHERE account_id <> ''
-UNION SELECT account_id, '' FROM usage_records WHERE account_id <> ''
-UNION SELECT account_id, '' FROM billing_holds WHERE account_id <> ''
-UNION SELECT account_id, '' FROM affinity_bindings WHERE account_id <> ''
-UNION SELECT account_id, '' FROM affinity_aliases WHERE account_id <> ''
-UNION SELECT account_id, '' FROM codex_upstream_attempt WHERE account_id <> ''
-UNION SELECT account_id, '' FROM codex_upstream_attempt_daily WHERE account_id <> ''`)
+SELECT account_id, MAX(account_label) FROM (
+	SELECT account_id,account_label FROM audit_log
+	WHERE account_id<>'' ORDER BY id DESC LIMIT ?
+) recent_audit GROUP BY account_id
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM cf_events WHERE account_id<>'' ORDER BY id DESC LIMIT ?
+) recent_cf
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM usage_records WHERE account_id<>'' ORDER BY id DESC LIMIT ?
+) recent_usage
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM billing_holds WHERE account_id<>'' ORDER BY created_at DESC,id DESC LIMIT ?
+) recent_holds
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM affinity_bindings WHERE account_id<>'' ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
+) recent_bindings
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM affinity_aliases WHERE account_id<>'' ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
+) recent_aliases
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM codex_upstream_attempt WHERE account_id<>'' ORDER BY created_at DESC,id DESC LIMIT ?
+) recent_attempts
+UNION SELECT account_id,'' FROM (
+	SELECT account_id FROM codex_upstream_attempt_daily WHERE account_id<>'' ORDER BY day_start DESC LIMIT ?
+) recent_daily`,
+		diagnosticExportRowLimit, diagnosticExportRowLimit,
+		diagnosticExportRowLimit, diagnosticExportRowLimit,
+		diagnosticExportRowLimit, diagnosticExportRowLimit,
+		diagnosticExportRowLimit, diagnosticExportRowLimit,
+	)
 	if err != nil {
 		return diagnosticCodebook{}, err
 	}
@@ -821,9 +866,13 @@ AND NOT EXISTS (SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_
 	out := map[string]diagnosticExportStat{}
 	for file, query := range queries {
 		var stat diagnosticExportStat
-		if err := db.QueryRowContext(ctx, query.sql, query.args...).Scan(&stat.Rows, &stat.Min, &stat.Max); err != nil {
+		if err := db.QueryRowContext(ctx, query.sql, query.args...).Scan(&stat.SourceRows, &stat.Min, &stat.Max); err != nil {
 			return nil, err
 		}
+		stat.Rows = stat.SourceRows
+		if stat.Rows > diagnosticExportRowLimit {
+			stat.Rows = diagnosticExportRowLimit
+		}
 		out[file] = stat
 	}
 	return out, nil
@@ -859,7 +908,10 @@ func streamDiagnosticEventsCSV(
 	return streamDiagnosticCSV(zw, "diagnostic_events.csv", header, func(cw *csv.Writer) (int64, error) {
 		rows, err := db.QueryContext(ctx, `
 SELECT id,event_type,severity,entity_type,entity_alias,detail_json,diagnostic_gap,created_at
-FROM diagnostic_events ORDER BY created_at,id`)
+FROM (
+	SELECT id,event_type,severity,entity_type,entity_alias,detail_json,diagnostic_gap,created_at
+	FROM diagnostic_events ORDER BY created_at DESC,id DESC LIMIT ?
+) recent ORDER BY created_at,id`, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1003,7 +1055,10 @@ func diagnosticEventTypedStringField(field string) bool {
 
 func streamDiagnosticAuditCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook) (int64, error) {
 	return streamDiagnosticCSV(zw, "audit_log.csv", []string{"id", "created_at", "account_code", "action", "state", "reason", "detail"}, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, `SELECT id, account_id, account_label, action, state, reason, detail, created_at FROM audit_log ORDER BY id`)
+		rows, err := db.QueryContext(ctx, `SELECT id,account_id,account_label,action,state,reason,detail,created_at FROM (
+	SELECT id,account_id,account_label,action,state,reason,detail,created_at
+	FROM audit_log ORDER BY id DESC LIMIT ?
+) recent ORDER BY id`, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1025,7 +1080,10 @@ func streamDiagnosticAuditCSV(ctx context.Context, db storage.ReadQuerier, zw *z
 
 func streamDiagnosticCFCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook) (int64, error) {
 	return streamDiagnosticCSV(zw, "cf_events.csv", []string{"id", "created_at", "account_code", "egress_id", "status", "cf_ray", "category", "message"}, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, `SELECT id, account_id, egress_id, status, cf_ray, category, message, created_at FROM cf_events ORDER BY id`)
+		rows, err := db.QueryContext(ctx, `SELECT id,account_id,egress_id,status,cf_ray,category,message,created_at FROM (
+	SELECT id,account_id,egress_id,status,cf_ray,category,message,created_at
+	FROM cf_events ORDER BY id DESC LIMIT ?
+) recent ORDER BY id`, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1048,7 +1106,8 @@ func streamDiagnosticCFCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.
 func streamDiagnosticUsageCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook) (int64, error) {
 	header := []string{"id", "created_at", "account_code", "route_key_hash", "api_key_hash", "user_id", "model", "prompt_tokens", "completion_tokens", "total_tokens", "cached_tokens", "cache_read_tokens", "cache_creation_tokens", "usage_provider", "usage_source", "cache_read_present", "cache_creation_present", "compatibility_losses_json", "cache_capability", "estimated", "cache_miss_tokens", "cache_total_input_tokens", "cache_creation_5m_tokens", "cache_creation_1h_tokens", "affinity_source", "route_class", "prompt_cache_key_present", "prompt_cache_key_source", "stable_prefix_source", "stable_prefix_reason", "stable_prefix_bytes", "retention_effective", "retention_source", "claude_cache_ttl", "cache_control_injected", "cache_breakpoint_count", "cache_breakpoints_json", "unwritten_tail_tokens", "max_possible_cache_read_tokens", "cache_hit_after_prewarm", "singleflight_waited_requests", "diagnostics_miss_reason", "latest_user_cache_control", "latest_user_auto_context_cache_control", "latest_user_tail_cache_control", "latest_user_tool_result_cache_control", "route_epoch", "raw_usage_json", "kiro_credits", "kiro_credits_present", "billing_hold_id", "requested_model", "resolved_model", "model_override_source"}
 	return streamDiagnosticCSV(zw, "usage_records.csv", header, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, diagnosticUsageRecordSelectSQL()+` ORDER BY id`)
+		rows, err := db.QueryContext(ctx, `SELECT * FROM (`+diagnosticUsageRecordSelectSQL()+`
+ ORDER BY id DESC LIMIT ?) recent ORDER BY id`, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1070,7 +1129,10 @@ func streamDiagnosticUsageCSV(ctx context.Context, db storage.ReadQuerier, zw *z
 
 func streamDiagnosticBillingHoldsCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook) (int64, error) {
 	return streamDiagnosticCSV(zw, "billing_holds.csv", []string{"id", "created_at", "updated_at", "account_code", "route_key_hash", "estimated_tokens", "status", "usage_expected", "usage_recorded_at"}, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, `SELECT id, route_key_hash, account_id, estimated_tokens, status, usage_expected, usage_recorded_at, created_at, updated_at FROM billing_holds ORDER BY created_at, id`)
+		rows, err := db.QueryContext(ctx, `SELECT id,route_key_hash,account_id,estimated_tokens,status,usage_expected,usage_recorded_at,created_at,updated_at FROM (
+	SELECT id,route_key_hash,account_id,estimated_tokens,status,usage_expected,usage_recorded_at,created_at,updated_at
+	FROM billing_holds ORDER BY created_at DESC,id DESC LIMIT ?
+) recent ORDER BY created_at,id`, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1094,13 +1156,15 @@ func streamDiagnosticAffinityCSV(ctx context.Context, db storage.ReadQuerier, zw
 	header := []string{"route_key_hash", "route_key", "source", "account_code", "provider", "model", "egress_id", "epoch", "created_at", "updated_at"}
 	return streamDiagnosticCSV(zw, "affinity_bindings.csv", header, func(cw *csv.Writer) (int64, error) {
 		rows, err := db.QueryContext(ctx, `SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM (
-	SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM affinity_aliases WHERE expires_at>?
-	UNION ALL
-	SELECT b.route_key_hash,b.route_key,b.source,b.account_id,b.provider,b.model,b.egress_id,b.epoch,b.created_at,b.updated_at FROM affinity_bindings b
-	WHERE (b.expires_at=0 OR b.expires_at>?) AND NOT EXISTS (
-		SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_key_hash AND a.expires_at>?
-	)
-	) ORDER BY updated_at,route_key_hash`, snapshotNow, snapshotNow, snapshotNow)
+	SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM (
+		SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM affinity_aliases WHERE expires_at>?
+		UNION ALL
+		SELECT b.route_key_hash,b.route_key,b.source,b.account_id,b.provider,b.model,b.egress_id,b.epoch,b.created_at,b.updated_at FROM affinity_bindings b
+		WHERE (b.expires_at=0 OR b.expires_at>?) AND NOT EXISTS (
+			SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_key_hash AND a.expires_at>?
+		)
+	) active ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
+) recent ORDER BY updated_at,route_key_hash`, snapshotNow, snapshotNow, snapshotNow, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1123,7 +1187,10 @@ func streamDiagnosticAffinityCSV(ctx context.Context, db storage.ReadQuerier, zw
 func streamDiagnosticCodexUpstreamAttemptsCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook, store *storage.Store, snapshotNow int64) (int64, error) {
 	header := []string{"tree_hmac_prefix", "account_code", "egress_id", "epoch", "state", "status_code", "created_at"}
 	return streamDiagnosticCSV(zw, "codex_upstream_attempts.csv", header, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, `SELECT tree_id,account_id,egress_id,epoch,state,status_code,created_at FROM codex_upstream_attempt WHERE expires_at>? ORDER BY created_at,id`, snapshotNow)
+		rows, err := db.QueryContext(ctx, `SELECT tree_id,account_id,egress_id,epoch,state,status_code,created_at FROM (
+	SELECT id,tree_id,account_id,egress_id,epoch,state,status_code,created_at
+	FROM codex_upstream_attempt WHERE expires_at>? ORDER BY created_at DESC,id DESC LIMIT ?
+) recent ORDER BY created_at,id`, snapshotNow, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
@@ -1148,8 +1215,11 @@ func streamDiagnosticCodexUpstreamAttemptsCSV(ctx context.Context, db storage.Re
 func streamDiagnosticCodexUpstreamAttemptsDailyCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.Writer, codebook diagnosticCodebook, snapshotNow int64) (int64, error) {
 	header := []string{"day_start", "account_code", "egress_id", "state", "status_code", "attempt_count", "first_created_at", "last_created_at"}
 	return streamDiagnosticCSV(zw, "codex_upstream_attempts_daily.csv", header, func(cw *csv.Writer) (int64, error) {
-		rows, err := db.QueryContext(ctx, `SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at
-FROM codex_upstream_attempt_daily WHERE expires_at>? ORDER BY day_start,account_id,egress_id,state,status_code`, snapshotNow)
+		rows, err := db.QueryContext(ctx, `SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at FROM (
+	SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at
+	FROM codex_upstream_attempt_daily WHERE expires_at>?
+	ORDER BY day_start DESC,account_id DESC,egress_id DESC,state DESC,status_code DESC LIMIT ?
+) recent ORDER BY day_start,account_id,egress_id,state,status_code`, snapshotNow, diagnosticExportRowLimit)
 		if err != nil {
 			return 0, err
 		}
diff --git a/internal/api/diagnostics_export_test.go b/internal/api/diagnostics_export_test.go
index 606aa7f..b93e54d 100644
--- a/internal/api/diagnostics_export_test.go
+++ b/internal/api/diagnostics_export_test.go
@@ -34,7 +34,10 @@ func awaitLegacyDiagnosticExport(t *testing.T, h *testHarness) []byte {
 	if err := json.Unmarshal(raw, &envelope); err != nil || envelope.Job.ID == "" {
 		t.Fatalf("decode diagnostics job: %v (%s)", err, raw)
 	}
-	deadline := time.Now().Add(15 * time.Second)
+	// Large capped-table fixtures are intentionally exercised under -race on the
+	// shared CI profile. Instrumented SQLite/ZIP/DLP work can exceed 15 seconds
+	// there even though the normal build completes in a few seconds.
+	deadline := time.Now().Add(60 * time.Second)
 	for time.Now().Before(deadline) {
 		job, err := h.store.GetDiagnosticJob(context.Background(), envelope.Job.ID)
 		if err != nil {
@@ -317,6 +320,74 @@ func TestAdminDiagnosticsExportAnonymizesBusinessLogs(t *testing.T) {
 	}
 }
 
+func TestDiagnosticsExportCapsAppendHeavyTablesToRecentRows(t *testing.T) {
+	h := newHarness(t, func(w http.ResponseWriter, r *http.Request) {})
+	ctx := context.Background()
+	var beforeRows, beforeMaxID int64
+	if err := h.store.ReadDB().QueryRowContext(ctx, `SELECT COUNT(*),COALESCE(MAX(id),0) FROM audit_log`).Scan(&beforeRows, &beforeMaxID); err != nil {
+		t.Fatal(err)
+	}
+	const extraRows int64 = 7
+	insertRows := diagnosticExportRowLimit + extraRows
+	if _, err := h.store.DB().ExecContext(ctx, `
+WITH RECURSIVE rows(value) AS (
+	VALUES(1) UNION ALL SELECT value+1 FROM rows WHERE value<?
+)
+INSERT INTO audit_log(account_id,account_label,action,state,reason,detail,created_at)
+SELECT 'historical-account-'||(value%10),'','request','alive','ok','row='||value,?
+FROM rows`, insertRows, storage.Now()); err != nil {
+		t.Fatal(err)
+	}
+
+	raw := awaitLegacyDiagnosticExport(t, h)
+	files := readZipFiles(t, raw)
+	var manifest struct {
+		Rows       map[string]int64 `json:"row_counts"`
+		SourceRows map[string]int64 `json:"source_row_counts"`
+		Truncated  map[string]struct {
+			SourceRows   int64  `json:"source_rows"`
+			ExportedRows int64  `json:"exported_rows"`
+			OmittedRows  int64  `json:"omitted_rows"`
+			Selection    string `json:"selection"`
+		} `json:"truncated_tables"`
+		RowLimit int64 `json:"large_table_row_limit"`
+	}
+	if err := json.Unmarshal([]byte(files["manifest.json"]), &manifest); err != nil {
+		t.Fatal(err)
+	}
+	wantSource := beforeRows + insertRows
+	if manifest.RowLimit != diagnosticExportRowLimit ||
+		manifest.Rows["audit_log.csv"] != diagnosticExportRowLimit ||
+		manifest.SourceRows["audit_log.csv"] != wantSource {
+		t.Fatalf("manifest cap metadata=%+v", manifest)
+	}
+	truncated := manifest.Truncated["audit_log.csv"]
+	if truncated.SourceRows != wantSource ||
+		truncated.ExportedRows != diagnosticExportRowLimit ||
+		truncated.OmittedRows != wantSource-diagnosticExportRowLimit ||
+		truncated.Selection != "most_recent" {
+		t.Fatalf("audit truncation metadata=%+v", truncated)
+	}
+	rows, err := csv.NewReader(strings.NewReader(files["audit_log.csv"])).ReadAll()
+	if err != nil {
+		t.Fatal(err)
+	}
+	if got := int64(len(rows) - 1); got != diagnosticExportRowLimit {
+		t.Fatalf("exported audit rows=%d want=%d", got, diagnosticExportRowLimit)
+	}
+	firstID, err := strconv.ParseInt(rows[1][0], 10, 64)
+	if err != nil {
+		t.Fatal(err)
+	}
+	lastID, err := strconv.ParseInt(rows[len(rows)-1][0], 10, 64)
+	if err != nil {
+		t.Fatal(err)
+	}
+	if firstID != beforeMaxID+extraRows+1 || lastID != beforeMaxID+insertRows {
+		t.Fatalf("export did not keep newest rows: first=%d last=%d before=%d inserted=%d", firstID, lastID, beforeMaxID, insertRows)
+	}
+}
+
 func TestDiagnosticSanitizerPreservesSchemaAndEnumText(t *testing.T) {
 	codebook := buildDiagnosticCodebookWithKey(
 		[]byte("stable-diagnostic-alias-key"),
diff --git a/internal/api/middleware.go b/internal/api/middleware.go
index d31109c..4037a03 100644
--- a/internal/api/middleware.go
+++ b/internal/api/middleware.go
@@ -120,6 +120,11 @@ func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
 	setSecurityHeaders(w, r)
 
 	rec := &responseRecorder{ResponseWriter: w, status: http.StatusOK}
+	requestBytes := r.ContentLength
+	metricRoute := s.httpMetrics.begin(r.URL.Path)
+	defer func() {
+		s.httpMetrics.finish(metricRoute, rec.status, requestBytes, rec.bytes, time.Since(start))
+	}()
 	if s.rejectForStoragePressure(r) {
 		writePublicServiceUnavailable(rec)
 		return
diff --git a/internal/api/request_metrics.go b/internal/api/request_metrics.go
new file mode 100644
index 0000000..91119a4
--- /dev/null
+++ b/internal/api/request_metrics.go
@@ -0,0 +1,264 @@
+package api
+
+import (
+	"fmt"
+	"net/http"
+	"strconv"
+	"strings"
+	"sync/atomic"
+	"time"
+)
+
+// HTTP request metrics deliberately use fixed arrays rather than maps. This keeps
+// observation O(1), allocation-free, and immune to unbounded label cardinality from
+// user-controlled paths, account IDs, models, or status codes.
+const (
+	httpRouteInference = iota
+	httpRouteAdmin
+	httpRouteAuth
+	httpRouteUser
+	httpRouteHealth
+	httpRouteOther
+	httpRouteCount
+)
+
+const (
+	httpStatus1xx = iota
+	httpStatus2xx
+	httpStatus3xx
+	httpStatus4xx
+	httpStatus5xx
+	httpStatusOther
+	httpStatusClassCount
+)
+
+var (
+	httpRouteNames = [...]string{
+		"inference",
+		"admin",
+		"auth",
+		"user",
+		"health",
+		"other",
+	}
+	httpStatusClassNames = [...]string{
+		"1xx",
+		"2xx",
+		"3xx",
+		"4xx",
+		"5xx",
+		"other",
+	}
+	httpLatencyUpperBounds = [...]time.Duration{
+		time.Millisecond,
+		5 * time.Millisecond,
+		10 * time.Millisecond,
+		25 * time.Millisecond,
+		50 * time.Millisecond,
+		100 * time.Millisecond,
+		250 * time.Millisecond,
+		500 * time.Millisecond,
+		time.Second,
+		2500 * time.Millisecond,
+		5 * time.Second,
+		10 * time.Second,
+		30 * time.Second,
+		0, // +Inf
+	}
+)
+
+type httpRouteMetrics struct {
+	inflight      atomic.Int64
+	requests      atomic.Uint64
+	requestBytes  atomic.Uint64
+	responseBytes atomic.Uint64
+	durationNanos atomic.Uint64
+	statuses      [httpStatusClassCount]atomic.Uint64
+	latencies     [len(httpLatencyUpperBounds)]atomic.Uint64
+}
+
+type httpRequestMetrics struct {
+	routes [httpRouteCount]httpRouteMetrics
+}
+
+type httpLatencyBucketSnapshot struct {
+	UpperBoundMillis int64  `json:"upper_bound_ms"`
+	Count            uint64 `json:"count"`
+}
+
+type httpRouteMetricsSnapshot struct {
+	Route         string                      `json:"route"`
+	Inflight      int64                       `json:"inflight"`
+	Requests      uint64                      `json:"requests"`
+	RequestBytes  uint64                      `json:"request_bytes"`
+	ResponseBytes uint64                      `json:"response_bytes"`
+	DurationNanos uint64                      `json:"duration_nanos"`
+	StatusClasses map[string]uint64           `json:"status_classes"`
+	Latency       []httpLatencyBucketSnapshot `json:"latency_buckets"`
+}
+
+type httpRequestMetricsSnapshot struct {
+	Inflight      int64                      `json:"inflight"`
+	Requests      uint64                     `json:"requests"`
+	RequestBytes  uint64                     `json:"request_bytes"`
+	ResponseBytes uint64                     `json:"response_bytes"`
+	Routes        []httpRouteMetricsSnapshot `json:"routes"`
+}
+
+func classifyHTTPRoute(path string) int {
+	switch {
+	case isInferenceRequestPath(path), path == "/v1/models", path == "/v1/gateway/identity":
+		return httpRouteInference
+	case strings.HasPrefix(path, "/admin/"):
+		return httpRouteAdmin
+	case strings.HasPrefix(path, "/auth/"):
+		return httpRouteAuth
+	case strings.HasPrefix(path, "/user/"):
+		return httpRouteUser
+	case path == "/healthz":
+		return httpRouteHealth
+	default:
+		return httpRouteOther
+	}
+}
+
+func classifyHTTPStatus(status int) int {
+	switch status / 100 {
+	case 1:
+		return httpStatus1xx
+	case 2:
+		return httpStatus2xx
+	case 3:
+		return httpStatus3xx
+	case 4:
+		return httpStatus4xx
+	case 5:
+		return httpStatus5xx
+	default:
+		return httpStatusOther
+	}
+}
+
+func (m *httpRequestMetrics) begin(path string) int {
+	route := classifyHTTPRoute(path)
+	m.routes[route].inflight.Add(1)
+	return route
+}
+
+func (m *httpRequestMetrics) finish(route, status int, requestBytes, responseBytes int64, elapsed time.Duration) {
+	if route < 0 || route >= len(m.routes) {
+		route = httpRouteOther
+	}
+	current := &m.routes[route]
+	current.inflight.Add(-1)
+	current.requests.Add(1)
+	if requestBytes > 0 {
+		current.requestBytes.Add(uint64(requestBytes))
+	}
+	if responseBytes > 0 {
+		current.responseBytes.Add(uint64(responseBytes))
+	}
+	if elapsed < 0 {
+		elapsed = 0
+	}
+	current.durationNanos.Add(uint64(elapsed))
+	current.statuses[classifyHTTPStatus(status)].Add(1)
+	for i, upper := range httpLatencyUpperBounds {
+		if upper == 0 || elapsed <= upper {
+			current.latencies[i].Add(1)
+			break
+		}
+	}
+}
+
+func (m *httpRequestMetrics) snapshot() httpRequestMetricsSnapshot {
+	out := httpRequestMetricsSnapshot{Routes: make([]httpRouteMetricsSnapshot, 0, httpRouteCount)}
+	for route, name := range httpRouteNames {
+		current := &m.routes[route]
+		row := httpRouteMetricsSnapshot{
+			Route:         name,
+			Inflight:      current.inflight.Load(),
+			Requests:      current.requests.Load(),
+			RequestBytes:  current.requestBytes.Load(),
+			ResponseBytes: current.responseBytes.Load(),
+			DurationNanos: current.durationNanos.Load(),
+			StatusClasses: make(map[string]uint64, httpStatusClassCount),
+			Latency:       make([]httpLatencyBucketSnapshot, len(httpLatencyUpperBounds)),
+		}
+		for i, class := range httpStatusClassNames {
+			row.StatusClasses[class] = current.statuses[i].Load()
+		}
+		for i, upper := range httpLatencyUpperBounds {
+			upperMillis := int64(-1)
+			if upper > 0 {
+				upperMillis = upper.Milliseconds()
+			}
+			row.Latency[i] = httpLatencyBucketSnapshot{
+				UpperBoundMillis: upperMillis,
+				Count:            current.latencies[i].Load(),
+			}
+		}
+		out.Inflight += row.Inflight
+		out.Requests += row.Requests
+		out.RequestBytes += row.RequestBytes
+		out.ResponseBytes += row.ResponseBytes
+		out.Routes = append(out.Routes, row)
+	}
+	return out
+}
+
+// adminMetrics exposes a bounded-cardinality Prometheus text surface for operators.
+// It shares the existing admin authentication path and never includes model, account,
+// key, tenant, request path, or other user-controlled labels.
+func (s *Server) adminMetrics(w http.ResponseWriter, r *http.Request) {
+	if !s.adminAllowed(w, r) {
+		return
+	}
+	if r.Method != http.MethodGet {
+		methodNotAllowed(w)
+		return
+	}
+
+	snapshot := s.httpMetrics.snapshot()
+	var out strings.Builder
+	out.Grow(16 << 10)
+	out.WriteString("# HELP codex_pool_http_requests_total Completed HTTP requests.\n")
+	out.WriteString("# TYPE codex_pool_http_requests_total counter\n")
+	out.WriteString("# HELP codex_pool_http_inflight_requests Current in-flight HTTP requests.\n")
+	out.WriteString("# TYPE codex_pool_http_inflight_requests gauge\n")
+	out.WriteString("# HELP codex_pool_http_request_size_bytes_total Accepted request body bytes when known.\n")
+	out.WriteString("# TYPE codex_pool_http_request_size_bytes_total counter\n")
+	out.WriteString("# HELP codex_pool_http_response_size_bytes_total Response body bytes written through HTTP.\n")
+	out.WriteString("# TYPE codex_pool_http_response_size_bytes_total counter\n")
+	out.WriteString("# HELP codex_pool_http_request_duration_seconds End-to-end HTTP request duration.\n")
+	out.WriteString("# TYPE codex_pool_http_request_duration_seconds histogram\n")
+
+	for _, route := range snapshot.Routes {
+		for _, class := range httpStatusClassNames {
+			fmt.Fprintf(&out, "codex_pool_http_requests_total{route=%q,status_class=%q} %d\n",
+				route.Route, class, route.StatusClasses[class])
+		}
+		fmt.Fprintf(&out, "codex_pool_http_inflight_requests{route=%q} %d\n", route.Route, route.Inflight)
+		fmt.Fprintf(&out, "codex_pool_http_request_size_bytes_total{route=%q} %d\n", route.Route, route.RequestBytes)
+		fmt.Fprintf(&out, "codex_pool_http_response_size_bytes_total{route=%q} %d\n", route.Route, route.ResponseBytes)
+
+		var cumulative uint64
+		for _, bucket := range route.Latency {
+			cumulative += bucket.Count
+			le := "+Inf"
+			if bucket.UpperBoundMillis >= 0 {
+				le = strconv.FormatFloat(float64(bucket.UpperBoundMillis)/1000, 'f', -1, 64)
+			}
+			fmt.Fprintf(&out, "codex_pool_http_request_duration_seconds_bucket{route=%q,le=%q} %d\n",
+				route.Route, le, cumulative)
+		}
+		fmt.Fprintf(&out, "codex_pool_http_request_duration_seconds_sum{route=%q} %s\n",
+			route.Route, strconv.FormatFloat(float64(route.DurationNanos)/float64(time.Second), 'f', -1, 64))
+		fmt.Fprintf(&out, "codex_pool_http_request_duration_seconds_count{route=%q} %d\n",
+			route.Route, route.Requests)
+	}
+	w.Header().Set("Content-Type", "text/plain; version=0.0.4; charset=utf-8")
+	w.Header().Set("Cache-Control", "no-store")
+	w.WriteHeader(http.StatusOK)
+	_, _ = w.Write([]byte(out.String()))
+}
diff --git a/internal/api/request_metrics_test.go b/internal/api/request_metrics_test.go
new file mode 100644
index 0000000..d97a3e4
--- /dev/null
+++ b/internal/api/request_metrics_test.go
@@ -0,0 +1,81 @@
+package api
+
+import (
+	"net/http"
+	"strings"
+	"testing"
+	"time"
+)
+
+func TestHTTPRequestMetricsFixedCardinalitySnapshot(t *testing.T) {
+	var metrics httpRequestMetrics
+	route := metrics.begin("/v1/responses")
+	metrics.finish(route, http.StatusTooManyRequests, 128, 64, 125*time.Millisecond)
+	route = metrics.begin("/admin/system")
+	metrics.finish(route, http.StatusOK, -1, 512, 2*time.Millisecond)
+
+	snapshot := metrics.snapshot()
+	if snapshot.Inflight != 0 || snapshot.Requests != 2 {
+		t.Fatalf("snapshot totals = %+v", snapshot)
+	}
+	if len(snapshot.Routes) != httpRouteCount {
+		t.Fatalf("routes = %d, want fixed %d", len(snapshot.Routes), httpRouteCount)
+	}
+	inference := snapshot.Routes[httpRouteInference]
+	if inference.Route != "inference" || inference.Requests != 1 ||
+		inference.RequestBytes != 128 || inference.ResponseBytes != 64 ||
+		inference.StatusClasses["4xx"] != 1 {
+		t.Fatalf("inference snapshot = %+v", inference)
+	}
+	var bucketTotal uint64
+	for _, bucket := range inference.Latency {
+		bucketTotal += bucket.Count
+	}
+	if bucketTotal != inference.Requests {
+		t.Fatalf("exclusive latency buckets total = %d, requests = %d", bucketTotal, inference.Requests)
+	}
+}
+
+func TestAdminMetricsPrometheusOutputIsBoundedAndAuthenticated(t *testing.T) {
+	h := newHarness(t, func(w http.ResponseWriter, r *http.Request) {
+		w.WriteHeader(http.StatusNoContent)
+	})
+
+	code, _ := grpReq(t, h, http.MethodGet, "/healthz", "")
+	if code != http.StatusOK {
+		t.Fatalf("healthz = %d, want 200", code)
+	}
+	code, raw := grpReq(t, h, http.MethodGet, "/admin/metrics", "")
+	if code != http.StatusOK {
+		t.Fatalf("admin metrics = %d, want 200: %s", code, raw)
+	}
+	body := string(raw)
+	for _, want := range []string{
+		"# TYPE codex_pool_http_requests_total counter",
+		`codex_pool_http_requests_total{route="health",status_class="2xx"} 1`,
+		`codex_pool_http_request_duration_seconds_bucket{route="health",le="+Inf"} 1`,
+	} {
+		if !strings.Contains(body, want) {
+			t.Fatalf("metrics missing %q:\n%s", want, body)
+		}
+	}
+	if strings.Contains(body, "/healthz") {
+		t.Fatalf("metrics leaked raw request path:\n%s", body)
+	}
+
+	h.app.cfg.AdminToken = "metrics-secret"
+	code, _ = grpReq(t, h, http.MethodGet, "/admin/metrics", "")
+	if code != http.StatusUnauthorized {
+		t.Fatalf("unauthenticated admin metrics = %d, want 401", code)
+	}
+}
+
+func BenchmarkHTTPRequestMetricsObserve(b *testing.B) {
+	var metrics httpRequestMetrics
+	b.ReportAllocs()
+	b.ResetTimer()
+	for i := 0; i < b.N; i++ {
+		route := metrics.begin("/v1/responses")
+		metrics.finish(route, http.StatusOK, 1024, 4096, 10*time.Millisecond)
+	}
+}
diff --git a/internal/api/server.go b/internal/api/server.go
index 882ff73..11daf20 100644
--- a/internal/api/server.go
+++ b/internal/api/server.go
@@ -91,6 +91,9 @@ type Server struct {
 	// clientErrors throttles browser-side error reports so the diagnostics endpoint
 	// cannot amplify a frontend fault or unauthenticated request flood into log spam.
 	clientErrors *clientErrorLimiter
+	// httpMetrics is fixed-cardinality and allocation-free on the request path.
+	// Operators can inspect it in /admin/system or scrape /admin/metrics.
+	httpMetrics httpRequestMetrics
 	// relState holds per-conversation working_state for the gateway reliability layer
 	// (in-memory + TTL'd + size-bounded; same pattern as oauth/login). Only written
 	// when the gateway_reliability flag is on. See reliability.go.
@@ -410,6 +413,7 @@ func (s *Server) routes() {
 	s.mux.HandleFunc("/admin/quota", s.adminQuota)
 	// Host + registration-task resource metrics (CPU/mem/disk + node/Chrome/Xvfb RSS).
 	s.mux.HandleFunc("/admin/system", s.adminSystem)
+	s.mux.HandleFunc("/admin/metrics", s.adminMetrics)
 	s.mux.HandleFunc("/admin/settings", s.adminSettings)
 	// /admin/config is the registry-backed System-config surface (requirement #1):
 	// GET returns every runtime-editable field with its effective value + metadata;
@@ -2432,25 +2436,6 @@ codexSuccess:
 						// prevents its terminal [DONE] write from retaining the WS request mutex.
 						_ = resp.Body.Close()
 					}
-					if shouldRetry && !codexResetRetried && !isAgentIdentityToken(token) && codexResetTriggerAllowed(failureStatus, failureBody) &&
-						s.tryAutoConsumeCodexResetCredit(r.Context(), lease.Account, token, lease.Egress, true, failureStatus, failureHeader, failureBody, "stream_retryable_limit") {
-						codexResetRetried = true
-						if latest, terr := s.store.GetToken(r.Context(), lease.Account.ID); terr == nil {
-							token = latest
-						}
-						retryResp, retryEgress, retryErr := s.doWithCFRetry(r.Context(), requestForToken(token), lease, mappingTransportStrict)
-						if retryErr == nil && retryResp.StatusCode < 400 {
-							resp = retryResp
-							finalEgress = retryEgress
-							goto codexSuccess
-						}
-						if retryResp != nil && retryResp.Body != nil {
-							_ = retryResp.Body.Close()
-						}
-						if retryErr != nil {
-							log.Printf("codex stream reset-credit retry %s: %v", lease.Account.ID, retryErr)
-						}
-					}
 					mapping := codexSessionMappingFromContext(r.Context())
 					rotationAllowed := !ruleMatched ||
 						decision.Match.DownstreamAction == upstreamrules.DownstreamActionBuiltin ||
@@ -2486,6 +2471,31 @@ codexSuccess:
 							RecoveryContextError: leakfilter.ResponsesContextErrorNone,
 						}
 					}
+					// A mapped, stateful turn with a healthy replacement account must
+					// rotate immediately. Polling quota/reset-credit endpoints first can
+					// block the downstream WebSocket for an entire upstream timeout and
+					// defeats the durable context failover that was already proven safe
+					// above. Reset credits remain the preferred in-place recovery when no
+					// mapped rotation is available.
+					if shouldRetry && !codexResetRetried && !isAgentIdentityToken(token) && codexResetTriggerAllowed(failureStatus, failureBody) &&
+						s.tryAutoConsumeCodexResetCredit(r.Context(), lease.Account, token, lease.Egress, true, failureStatus, failureHeader, failureBody, "stream_retryable_limit") {
+						codexResetRetried = true
+						if latest, terr := s.store.GetToken(r.Context(), lease.Account.ID); terr == nil {
+							token = latest
+						}
+						retryResp, retryEgress, retryErr := s.doWithCFRetry(r.Context(), requestForToken(token), lease, mappingTransportStrict)
+						if retryErr == nil && retryResp.StatusCode < 400 {
+							resp = retryResp
+							finalEgress = retryEgress
+							goto codexSuccess
+						}
+						if retryResp != nil && retryResp.Body != nil {
+							_ = retryResp.Body.Close()
+						}
+						if retryErr != nil {
+							log.Printf("codex stream reset-credit retry %s: %v", lease.Account.ID, retryErr)
+						}
+					}
 					// The early-frame probe has not committed any downstream bytes, so a
 					// stateful failover rule may make one safe retry through the exact
 					// same account+egress. Do not convert an ordinary repeated upstream
diff --git a/internal/api/system.go b/internal/api/system.go
index c5a2422..5a969b9 100644
--- a/internal/api/system.go
+++ b/internal/api/system.go
@@ -32,23 +32,25 @@ func (s *Server) adminSystem(w http.ResponseWriter, r *http.Request) {
 	dataDir := filepath.Dir(s.cfg.DatabasePath)
 	payload := struct {
 		sysmetrics.Metrics
-		Admission         interface{}               `json:"admission"`
-		Scheduler         interface{}               `json:"scheduler"`
-		BodyStorage       bodysource.BudgetSnapshot `json:"body_storage"`
-		UsageJournal      map[string]interface{}    `json:"usage_journal"`
-		ContextRebuilt    uint64                    `json:"context_rebuilt"`
-		ContextDegraded   uint64                    `json:"context_degraded"`
-		CodexSessionMap   map[string]interface{}    `json:"codex_session_mapping"`
-		Sidecar           interface{}               `json:"sidecar,omitempty"`
-		DiskGuard         DiskGuardSnapshot         `json:"disk_guard"`
-		SupervisorEvents  []supervisor.Event        `json:"supervisor_events"`
-		SupervisorModules []supervisor.ModuleState  `json:"supervisor_modules"`
+		Admission         interface{}                `json:"admission"`
+		Scheduler         interface{}                `json:"scheduler"`
+		BodyStorage       bodysource.BudgetSnapshot  `json:"body_storage"`
+		UsageJournal      map[string]interface{}     `json:"usage_journal"`
+		HTTP              httpRequestMetricsSnapshot `json:"http"`
+		ContextRebuilt    uint64                     `json:"context_rebuilt"`
+		ContextDegraded   uint64                     `json:"context_degraded"`
+		CodexSessionMap   map[string]interface{}     `json:"codex_session_mapping"`
+		Sidecar           interface{}                `json:"sidecar,omitempty"`
+		DiskGuard         DiskGuardSnapshot          `json:"disk_guard"`
+		SupervisorEvents  []supervisor.Event         `json:"supervisor_events"`
+		SupervisorModules []supervisor.ModuleState   `json:"supervisor_modules"`
 	}{
 		Metrics:        sysmetrics.Collect(dataDir),
 		Admission:      s.scheduler.AdmissionSnapshot(),
 		Scheduler:      s.scheduler.Metrics(),
 		BodyStorage:    s.bodyBudgetSnapshot(),
 		UsageJournal:   s.usageJournalMetrics(),
+		HTTP:           s.httpMetrics.snapshot(),
 		ContextRebuilt: atomic.LoadUint64(&s.contextRebuilt), ContextDegraded: atomic.LoadUint64(&s.contextDegraded),
 		CodexSessionMap:   s.codexSessionMappingStats(r.Context()),
 		Sidecar:           s.sidecarMetrics(r.Context()),
diff --git a/web-spa/src/features/observability/api/exports.ts b/web-spa/src/features/observability/api/exports.ts
index 66769b7..115154c 100644
--- a/web-spa/src/features/observability/api/exports.ts
+++ b/web-spa/src/features/observability/api/exports.ts
@@ -28,8 +28,8 @@ const archiveDefinitions: Record<Exclude<AuditExportKind, 'diagnostics'>, { endp
 
 const diagnosticFallbackName = 'codex-pool-diagnostics.zip';
 const diagnosticPollIntervalMs = 2_000;
-const diagnosticTimeoutMs = 30 * 60 * 1_000;
-const diagnosticQueuedStallMs = 60_000;
+const diagnosticTimeoutMs = 5 * 60 * 1_000;
+const diagnosticQueuedStallMs = 15_000;
 const pendingDiagnosticStatuses = new Set(['queued', 'snapshotting', 'rendering', 'validating']);
 const runningDiagnosticStatuses = new Set(['snapshotting', 'rendering', 'validating']);
 const terminalDiagnosticStatuses = new Set(['failed', 'cancelled', 'expired']);
@@ -117,7 +117,9 @@ async function fetchDirectArchive(
   endpoint: string,
   fallbackName: string,
 ): Promise<AuditArchive> {
-  const response = await api.get(endpoint, { responseType: 'arraybuffer' });
+  // Let the browser construct the Blob directly. Fetching an ArrayBuffer and then
+  // wrapping it in a Blob can briefly retain two full copies of a large archive.
+  const response = await api.get(endpoint, { responseType: 'blob' });
   return zipBlobFromResponse(response, fallbackName);
 }
 
@@ -183,7 +185,7 @@ async function fetchDiagnosticArchive(options: DiagnosticExportOptions = {}): Pr
     }
   }
 
-  const response = await api.get(`${jobPath}/download`, { responseType: 'arraybuffer' });
+  const response = await api.get(`${jobPath}/download`, { responseType: 'blob' });
   return zipBlobFromResponse(response, diagnosticFallbackName);
 }
 
diff --git a/web-spa/src/lib/browserDownload.js b/web-spa/src/lib/browserDownload.js
index b6f822d..4558821 100644
--- a/web-spa/src/lib/browserDownload.js
+++ b/web-spa/src/lib/browserDownload.js
@@ -1,3 +1,5 @@
+import { setBrowserTimeout } from './browserLifecycle.js';
+
 export function downloadBlob(name, blob) {
   if (!name || !blob || typeof document === 'undefined' || typeof URL === 'undefined') return false;
 
@@ -12,6 +14,29 @@ export function downloadBlob(name, blob) {
     anchor.style.cssText = 'position:fixed;left:-9999px;top:0;opacity:0';
     document.body.appendChild(anchor);
     anchor.click();
+    // Revoking synchronously after click races the browser's download
+    // navigation (notably Firefox/Safari) and can leave the UI reporting
+    // success without a file. Keep the object URL alive for one second; the
+    // anchor itself can still be removed immediately.
+    const completedURL = url;
+    url = '';
+    const cleanupTimer = setBrowserTimeout(() => {
+      try {
+        URL.revokeObjectURL(completedURL);
+      } catch {
+        // Object URL cleanup is best-effort after the download has started.
+      }
+    }, 1_000);
+    if (cleanupTimer == null) {
+      try {
+        // A browser without timers should not retain the Blob indefinitely.
+        // This rare fallback may be synchronous, but normal browsers always
+        // take the delayed path above.
+        URL.revokeObjectURL(completedURL);
+      } catch {
+        // Object URL cleanup is best-effort.
+      }
+    }
     return true;
   } catch {
     return false;
diff --git a/web-spa/tests/browser-download.test.ts b/web-spa/tests/browser-download.test.ts
new file mode 100644
index 0000000..2cc1815
--- /dev/null
+++ b/web-spa/tests/browser-download.test.ts
@@ -0,0 +1,28 @@
+import { afterEach, describe, expect, it, vi } from 'vitest';
+import { downloadBlob } from '../src/lib/browserDownload.js';
+
+describe('browser download', () => {
+  afterEach(() => {
+    vi.useRealTimers();
+  });
+
+  it('keeps the object URL alive until the browser has started the download', () => {
+    vi.useFakeTimers();
+    const createObjectURL = vi.fn(() => 'blob:diagnostic-archive');
+    const revokeObjectURL = vi.fn();
+    Object.defineProperty(URL, 'createObjectURL', { configurable: true, value: createObjectURL });
+    Object.defineProperty(URL, 'revokeObjectURL', { configurable: true, value: revokeObjectURL });
+    const click = vi.spyOn(HTMLAnchorElement.prototype, 'click').mockImplementation(() => undefined);
+
+    expect(downloadBlob('diagnostics.zip', new Blob(['PK\u0003\u0004']))).toBe(true);
+    expect(click).toHaveBeenCalledTimes(1);
+    expect(createObjectURL).toHaveBeenCalledTimes(1);
+    expect(revokeObjectURL).not.toHaveBeenCalled();
+    expect(document.querySelector('a[download="diagnostics.zip"]')).toBeNull();
+
+    vi.advanceTimersByTime(999);
+    expect(revokeObjectURL).not.toHaveBeenCalled();
+    vi.advanceTimersByTime(1);
+    expect(revokeObjectURL).toHaveBeenCalledWith('blob:diagnostic-archive');
+  });
+});
diff --git a/web-spa/tests/diagnostic-export.test.ts b/web-spa/tests/diagnostic-export.test.ts
index ac095cd..7a61042 100644
--- a/web-spa/tests/diagnostic-export.test.ts
+++ b/web-spa/tests/diagnostic-export.test.ts
@@ -48,7 +48,7 @@ describe('diagnostic archive export', () => {
     expect(api.get).toHaveBeenNthCalledWith(1, '/admin/diagnostics/jobs/diagjob_test123');
     expect(api.get).toHaveBeenNthCalledWith(2, '/admin/diagnostics/jobs/diagjob_test123');
     expect(api.get).toHaveBeenNthCalledWith(3, '/admin/diagnostics/jobs/diagjob_test123/download', {
-      responseType: 'arraybuffer',
+      responseType: 'blob',
     });
     expect(archive.filename).toBe('diagnostics-v3.zip');
     expect(Array.from(new Uint8Array(await archive.blob.slice(0, 4).arrayBuffer()))).toEqual([

Changes:

unknown
  @@ -0,0 +1,301 @@
  +# 项目全栈优化审计与实测报告（2026-07-29）
  +
  +## 1. 结论摘要
  +
  +本轮以“时间复杂度优先、真实链路验证”为准则，覆盖请求接入、记录与用量、上下文隔离、调度、上游转发、前端下载、管理面和运维面。
  +
  +已落地并验证：
  +
  +1. **诊断包下载卡转圈**
  +   - 追加型大表只导出最新 `20,000` 行，同时在 manifest 保留源表总行数、遗漏行数和截断策略。
  +   - 前端下载改用 `Blob`，缩短前端总等待上限至 5 分钟，队列无进展阈值降至 15 秒。
  +   - 浏览器对象 URL 延迟回收，避免 Firefox/Safari 在点击后同步回收导致下载被取消。
  +   - 70 万历史记录场景从 **110.714 秒降至 5.246 秒**，加速 **21.10 倍**，耗时下降 **95.26%**。
  +   - Chrome 真实 UI 点击 `/console/audit` 的“导出诊断包”后 **3.087 秒完成下载**，ZIP 签名正确，按钮恢复可用。
  +2. **WebSocket 配额切换停顿**
  +   - 对已有健康替代账号的映射会话，先执行持久上下文轮转，不再先同步等待配额重置探测。
  +   - 原用例每次 10.12–10.15 秒超时，修复后连续 5 次均为 0.13–0.15 秒并成功恢复工具调用上下文。
  +3. **运维可观测性**
  +   - 新增鉴权的 `GET /admin/metrics` Prometheus 文本端点。
  +   - 使用固定 6 类路由、6 类状态和固定延迟桶，不携带原始 path、模型、账号等高基数字段。
  +   - 请求观测基准为 **18.23–18.41 ns/op、0 B/op、0 allocs/op**。
  +4. **空态管理页**
  +   - `/admin/audit` 空结果稳定返回 `[]`，真实浏览器不再显示“数据读取失败”。
  +5. **最终回归**
  +   - Go 全量、`go vet`、目标竞态、核心竞态、前端完整验证、真实 Chrome 下载、70 万行诊断、TLS/HTTP2 日常负载均在共享远端服务器的隔离目录内执行。
  +
  +## 2. 测试约束与硬件
  +
  +远端硬件：
  +
  +| 项目 | 实测 |
  +|---|---:|
  +| CPU | AMD EPYC 9654，96 核 / 192 线程 |
  +| L3 | 384 MiB |
  +| 内存 | 377 GiB，无 Swap |
  +| 测试盘 | XFS，测试开始时约 60 GiB 可用 |
  +| 共享机负载 | load average 约 86–88 |
  +| 本轮限额 | 仅 CPU `190,191`，`GOMAXPROCS=2`，`nice -n 15`，`ionice -c3` |
  +
  +所有远端数据、工具链、浏览器、缓存、数据库和结果均位于：
  +
  +```text
  +/root/autodl-tmp/codex-pool-regression-20260729
  +```
  +
  +这保证本轮没有占用整机 CPU，也没有读写其他用户目录。
  +
  +### 硬件使用建议
  +
  +- **网关请求面**：GPU 对当前协议代理、JSON/SSE、调度和加密链路没有收益；优先高主频 CPU、稳定网络和低延迟 NVMe。
  +- **单机 SQLite**：建议 4–8 个受限 vCPU 起步，`GOMAXPROCS` 对齐容器 CPU quota；请求体内存预算和响应体预算必须小于容器内存上限并保留 20%–30% 余量。
  +- **高写入或多副本**：SQLite 单写者会先成为瓶颈；使用现有 PostgreSQL + active/standby + fencing 路径。
  +- **大请求**：保留内存/磁盘分层 body source，spool 目录应放在独立 NVMe/XFS，并监控磁盘余量、文件数和写入延迟。
  +- **共享/容器环境**：按 CPU quota 设置 `GOMAXPROCS`，按内存限制设置 `GOMEMLIMIT`；避免按宿主机 192 线程错误扩张工作队列。
  +
  +## 3. 架构与复杂度审计
  +
  +### 3.1 主链路
  +
  +```text
  +HTTP/WS 接入
  +  -> 鉴权、限流、请求体分层捕获
  +  -> 用户组/模型/Provider 策略
  +  -> CPA 会话映射与 Goal 连续性
  +  -> 调度索引、亲和性、账号/出口租约
  +  -> 上游 HTTP/SSE/WS 转发与重试
  +  -> 流式过滤、用量提取、异步日志/Journal
  +  -> 下游响应
  +```
  +
  +### 3.2 热路径复杂度
  +
  +符号：`B` 请求字节数，`S` 响应字节数，`N` 候选账号数，`H` 可恢复历史大小，`R` 历史记录数，`L=20,000` 为单张诊断大表导出上限。
  +
  +| 阶段 | 稳态时间 | 额外空间 | 结论 |
  +|---|---:|---:|---|
  +| 请求体捕获 | `O(B)` | `O(min(B, memory_threshold))`，其余落盘 | 已有内存/磁盘双预算与磁盘保留保护 |
  +| 路由候选索引构建 | `O(N)` | `O(N)` | 缓存构建，不放在每次采样热路径 |
  +| 请求候选采样 | `O(1)` | `O(1)` | 10、1,000、10,000 账号均约 13–15 ns、0 alloc |
  +| 亲和 Top-3 构建 | `O(N)` | `O(N)` | 缓存刷新阶段；10k 账号约 609 µs |
  +| 等待队列取消 | `O(1)` | `O(1)` | 侵入式移除，约 448–461 ns |
  +| CPA 别名查询 | 索引下 `O(log M)` | `O(1)`/结果集 | HMAC 命名空间，避免原始下游标识串号 |
  +| 上下文正常续接 | `O(1)` 元数据 + `O(B)` 转发 | 有界 | 原生 upstream session 为主路径 |
  +| 上下文故障重建 | `O(H+B)` | `O(H+B)` | 仅绑定丢失、上下文丢失或会话风险时触发 |
  +| HTTP/SSE/WS 转发 | `O(S)` | 固定缓冲/有界 spool | SSE buffer 复用、WS 顺序会话 |
  +| 用量提取 | `O(S)` | 有界 scanner | 请求后异步队列，批量最多 64 条/5 ms |
  +| Journal 追加 | 摊销 `O(1)` | 有界队列 | 崩溃恢复仍与待重放段大小线性相关 |
  +| 诊断大表行物化（修复前） | `O(R)` | archive 与扫描随 `R` 增长 | 直接导致前端长时间转圈 |
  +| 诊断大表行物化（修复后） | `O(min(R,L))` | `O(min(R,L))` | 最新 20k 行，DLP/ZIP 体积被上限约束 |
  +| 诊断 COUNT/聚合 | `O(R)`（数据库侧） | `O(1)` | **剩余 P1**：源行数、部分汇总仍扫描历史表 |
  +| HTTP 指标观测 | `O(1)` | 固定数组 | 18.23–18.41 ns，0 alloc，标签基数固定 |
  +
  +### 3.3 仍需优先处理的复杂度项
  +
  +1. **P1：诊断 metadata 的全表聚合**
  +   - 当前导出负载和 DLP 已受 `L` 限制，但精确 `COUNT(*)`、时间范围和部分审计汇总仍为 `O(R)`。
  +   - 建议按小时维护增量 rollup，并把精确扫描改为管理员显式“深度诊断”模式。
  +2. **P1：Journal 恢复游标**
  +   - 正常追加为摊销 `O(1)`；启动恢复仍需扫描待重放段。
  +   - 建议持久化已确认 segment/offset 游标，减少大故障恢复时间。
  ... (201 lines truncated)
  +301 -0

internal/api/admin_config.go
  @@ -791,6 +791,9 @@ func (s *Server) adminAudit(w http.ResponseWriter, r *http.Request) {
  +	if rows == nil {
  +		rows = []storage.AuditLogRow{}
  +	}
   	writeJSON(w, http.StatusOK, rows)
   }
   
  +3 -0

internal/api/audit_test.go
  @@ -4,6 +4,7 @@ import (
  +	"strings"
   	"testing"
   
   	"codex-account-pool/internal/storage"
  @@ -42,3 +43,15 @@ func TestAdminAuditCanFilterByAccountID(t *testing.T) {
  +
  +func TestAdminAuditEmptyReturnsJSONArray(t *testing.T) {
  +	h := newHarness(t, func(w http.ResponseWriter, r *http.Request) {})
  +
  +	code, raw := grpReq(t, h, http.MethodGet, "/admin/audit?limit=10", "")
  +	if code != http.StatusOK {
  +		t.Fatalf("admin audit = %d: %s", code, raw)
  +	}
  +	if strings.TrimSpace(string(raw)) != "[]" {
  +		t.Fatalf("empty audit response = %s, want []", raw)
  +	}
  +}
  +13 -0

internal/api/diagnostic_jobs.go
  @@ -28,7 +28,7 @@ import (
  -	diagnosticJobTimeout           = 30 * time.Minute
  +	diagnosticJobTimeout           = 5 * time.Minute
   	diagnosticArtifactTTL          = 24 * time.Hour
   	diagnosticJobPollInterval      = time.Second
   	diagnosticJobQueueCapacity     = 3 // one running + two queued
  +1 -1

internal/api/diagnostics_export.go
  @@ -192,11 +192,18 @@ func diagnosticFileOrder() []string {
  -	Rows int64
  -	Min  int64
  -	Max  int64
  +	Rows       int64
  +	SourceRows int64
  +	Min        int64
  +	Max        int64
   }
   
  +// A diagnostics bundle is a support snapshot, not a second full database
  +// backup. Keeping the newest rows from each append-heavy table makes export
  +// latency and memory/disk work predictable even after months of traffic. The
  +// manifest records the exact source/exported counts whenever this cap applies.
  +const diagnosticExportRowLimit int64 = 20_000
  +
   func (s *Server) streamDiagnosticsExport(ctx context.Context, w http.ResponseWriter) error {
   	snapshot, err := s.store.BeginDiagnosticSnapshot(ctx)
   	if err != nil {
  @@ -387,8 +394,19 @@ func (s *Server) writeDiagnosticsExport(ctx context.Context, dst io.Writer, snap
  +	sourceRowCounts := make(map[string]int64, len(stats))
  +	truncatedTables := map[string]map[string]interface{}{}
   	for name, stat := range stats {
   		rowCounts[name] = stat.Rows
  +		sourceRowCounts[name] = stat.SourceRows
  +		if stat.SourceRows > stat.Rows {
  +			truncatedTables[name] = map[string]interface{}{
  +				"source_rows":   stat.SourceRows,
  +				"exported_rows": stat.Rows,
  +				"omitted_rows":  stat.SourceRows - stat.Rows,
  +				"selection":     "most_recent",
  +			}
  +		}
   	}
   	rowCounts["manifest.json"] = 1
   	timeRanges := map[string]interface{}{}
  @@ -512,9 +530,14 @@ func (s *Server) writeDiagnosticsExport(ctx context.Context, dst io.Writer, snap
  -		"build": diagnosticBuildInfo(), "table_time_ranges": timeRanges,
  -		"account_redaction":   "all entity identifiers use type-isolated stable HMAC aliases; no reverse map or alias key is included",
  -		"account_code_format": "ACC-Base32(HMAC-SHA256(alias_key,domain||entity_type||raw_id)[:16])",
  +		"source_row_counts":       sourceRowCounts,
  +		"truncated_tables":        truncatedTables,
  +		"large_table_row_limit":   diagnosticExportRowLimit,
  +		"build":                   diagnosticBuildInfo(),
  +		"table_time_ranges":       timeRanges,
  +		"table_time_ranges_scope": "source_snapshot",
  +		"account_redaction":       "all entity identifiers use type-isolated stable HMAC aliases; no reverse map or alias key is included",
  +		"account_code_format":     "ACC-Base32(HMAC-SHA256(alias_key,domain||entity_type||raw_id)[:16])",
   	}
   	if err := addJSON("manifest.json", manifest); err != nil {
   		return err
  @@ -600,14 +623,36 @@ func (s *Server) codexSessionMappingStatsFromSnapshot(ctx context.Context, snaps
  -SELECT account_id, MAX(account_label) FROM audit_log WHERE account_id <> '' GROUP BY account_id
  -UNION SELECT account_id, '' FROM cf_events WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM usage_records WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM billing_holds WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM affinity_bindings WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM affinity_aliases WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM codex_upstream_attempt WHERE account_id <> ''
  -UNION SELECT account_id, '' FROM codex_upstream_attempt_daily WHERE account_id <> ''`)
  +SELECT account_id, MAX(account_label) FROM (
  +	SELECT account_id,account_label FROM audit_log
  +	WHERE account_id<>'' ORDER BY id DESC LIMIT ?
  +) recent_audit GROUP BY account_id
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM cf_events WHERE account_id<>'' ORDER BY id DESC LIMIT ?
  +) recent_cf
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM usage_records WHERE account_id<>'' ORDER BY id DESC LIMIT ?
  +) recent_usage
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM billing_holds WHERE account_id<>'' ORDER BY created_at DESC,id DESC LIMIT ?
  +) recent_holds
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM affinity_bindings WHERE account_id<>'' ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
  +) recent_bindings
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM affinity_aliases WHERE account_id<>'' ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
  +) recent_aliases
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM codex_upstream_attempt WHERE account_id<>'' ORDER BY created_at DESC,id DESC LIMIT ?
  +) recent_attempts
  +UNION SELECT account_id,'' FROM (
  +	SELECT account_id FROM codex_upstream_attempt_daily WHERE account_id<>'' ORDER BY day_start DESC LIMIT ?
  +) recent_daily`,
  +		diagnosticExportRowLimit, diagnosticExportRowLimit,
  +		diagnosticExportRowLimit, diagnosticExportRowLimit,
  +		diagnosticExportRowLimit, diagnosticExportRowLimit,
  +		diagnosticExportRowLimit, diagnosticExportRowLimit,
  +	)
   	if err != nil {
   		return diagnosticCodebook{}, err
   	}
  @@ -821,9 +866,13 @@ AND NOT EXISTS (SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_
  -		if err := db.QueryRowContext(ctx, query.sql, query.args...).Scan(&stat.Rows, &stat.Min, &stat.Max); err != nil {
  +		if err := db.QueryRowContext(ctx, query.sql, query.args...).Scan(&stat.SourceRows, &stat.Min, &stat.Max); err != nil {
   			return nil, err
   		}
  +		stat.Rows = stat.SourceRows
  +		if stat.Rows > diagnosticExportRowLimit {
  +			stat.Rows = diagnosticExportRowLimit
  +		}
   		out[file] = stat
   	}
   	return out, nil
  @@ -859,7 +908,10 @@ func streamDiagnosticEventsCSV(
  -FROM diagnostic_events ORDER BY created_at,id`)
  +FROM (
  +	SELECT id,event_type,severity,entity_type,entity_alias,detail_json,diagnostic_gap,created_at
  +	FROM diagnostic_events ORDER BY created_at DESC,id DESC LIMIT ?
  +) recent ORDER BY created_at,id`, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1003,7 +1055,10 @@ func diagnosticEventTypedStringField(field string) bool {
  -		rows, err := db.QueryContext(ctx, `SELECT id, account_id, account_label, action, state, reason, detail, created_at FROM audit_log ORDER BY id`)
  +		rows, err := db.QueryContext(ctx, `SELECT id,account_id,account_label,action,state,reason,detail,created_at FROM (
  +	SELECT id,account_id,account_label,action,state,reason,detail,created_at
  +	FROM audit_log ORDER BY id DESC LIMIT ?
  +) recent ORDER BY id`, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1025,7 +1080,10 @@ func streamDiagnosticAuditCSV(ctx context.Context, db storage.ReadQuerier, zw *z
  -		rows, err := db.QueryContext(ctx, `SELECT id, account_id, egress_id, status, cf_ray, category, message, created_at FROM cf_events ORDER BY id`)
  +		rows, err := db.QueryContext(ctx, `SELECT id,account_id,egress_id,status,cf_ray,category,message,created_at FROM (
  +	SELECT id,account_id,egress_id,status,cf_ray,category,message,created_at
  +	FROM cf_events ORDER BY id DESC LIMIT ?
  +) recent ORDER BY id`, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1048,7 +1106,8 @@ func streamDiagnosticCFCSV(ctx context.Context, db storage.ReadQuerier, zw *zip.
  -		rows, err := db.QueryContext(ctx, diagnosticUsageRecordSelectSQL()+` ORDER BY id`)
  +		rows, err := db.QueryContext(ctx, `SELECT * FROM (`+diagnosticUsageRecordSelectSQL()+`
  + ORDER BY id DESC LIMIT ?) recent ORDER BY id`, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1070,7 +1129,10 @@ func streamDiagnosticUsageCSV(ctx context.Context, db storage.ReadQuerier, zw *z
  -		rows, err := db.QueryContext(ctx, `SELECT id, route_key_hash, account_id, estimated_tokens, status, usage_expected, usage_recorded_at, created_at, updated_at FROM billing_holds ORDER BY created_at, id`)
  +		rows, err := db.QueryContext(ctx, `SELECT id,route_key_hash,account_id,estimated_tokens,status,usage_expected,usage_recorded_at,created_at,updated_at FROM (
  +	SELECT id,route_key_hash,account_id,estimated_tokens,status,usage_expected,usage_recorded_at,created_at,updated_at
  +	FROM billing_holds ORDER BY created_at DESC,id DESC LIMIT ?
  +) recent ORDER BY created_at,id`, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1094,13 +1156,15 @@ func streamDiagnosticAffinityCSV(ctx context.Context, db storage.ReadQuerier, zw
  -	SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM affinity_aliases WHERE expires_at>?
  -	UNION ALL
  -	SELECT b.route_key_hash,b.route_key,b.source,b.account_id,b.provider,b.model,b.egress_id,b.epoch,b.created_at,b.updated_at FROM affinity_bindings b
  -	WHERE (b.expires_at=0 OR b.expires_at>?) AND NOT EXISTS (
  -		SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_key_hash AND a.expires_at>?
  -	)
  -	) ORDER BY updated_at,route_key_hash`, snapshotNow, snapshotNow, snapshotNow)
  +	SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM (
  +		SELECT route_key_hash,route_key,source,account_id,provider,model,egress_id,epoch,created_at,updated_at FROM affinity_aliases WHERE expires_at>?
  +		UNION ALL
  +		SELECT b.route_key_hash,b.route_key,b.source,b.account_id,b.provider,b.model,b.egress_id,b.epoch,b.created_at,b.updated_at FROM affinity_bindings b
  +		WHERE (b.expires_at=0 OR b.expires_at>?) AND NOT EXISTS (
  +			SELECT 1 FROM affinity_aliases a WHERE a.route_key_hash=b.route_key_hash AND a.expires_at>?
  +		)
  +	) active ORDER BY updated_at DESC,route_key_hash DESC LIMIT ?
  +) recent ORDER BY updated_at,route_key_hash`, snapshotNow, snapshotNow, snapshotNow, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1123,7 +1187,10 @@ func streamDiagnosticAffinityCSV(ctx context.Context, db storage.ReadQuerier, zw
  -		rows, err := db.QueryContext(ctx, `SELECT tree_id,account_id,egress_id,epoch,state,status_code,created_at FROM codex_upstream_attempt WHERE expires_at>? ORDER BY created_at,id`, snapshotNow)
  +		rows, err := db.QueryContext(ctx, `SELECT tree_id,account_id,egress_id,epoch,state,status_code,created_at FROM (
  +	SELECT id,tree_id,account_id,egress_id,epoch,state,status_code,created_at
  +	FROM codex_upstream_attempt WHERE expires_at>? ORDER BY created_at DESC,id DESC LIMIT ?
  +) recent ORDER BY created_at,id`, snapshotNow, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  @@ -1148,8 +1215,11 @@ func streamDiagnosticCodexUpstreamAttemptsCSV(ctx context.Context, db storage.Re
  -		rows, err := db.QueryContext(ctx, `SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at
  -FROM codex_upstream_attempt_daily WHERE expires_at>? ORDER BY day_start,account_id,egress_id,state,status_code`, snapshotNow)
  +		rows, err := db.QueryContext(ctx, `SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at FROM (
  +	SELECT day_start,account_id,egress_id,state,status_code,attempt_count,first_created_at,last_created_at
  +	FROM codex_upstream_attempt_daily WHERE expires_at>?
  +	ORDER BY day_start DESC,account_id DESC,egress_id DESC,state DESC,status_code DESC LIMIT ?
  +) recent ORDER BY day_start,account_id,egress_id,state,status_code`, snapshotNow, diagnosticExportRowLimit)
   		if err != nil {
   			return 0, err
   		}
  +100 -30

internal/api/diagnostics_export_test.go
  @@ -34,7 +34,10 @@ func awaitLegacyDiagnosticExport(t *testing.T, h *testHarness) []byte {
  -	deadline := time.Now().Add(15 * time.Second)
  +	// Large capped-table fixtures are intentionally exercised under -race on the
  +	// shared CI profile. Instrumented SQLite/ZIP/DLP work can exceed 15 seconds
  +	// there even though the normal build completes in a few seconds.
  +	deadline := time.Now().Add(60 * time.Second)
   	for time.Now().Before(deadline) {
   		job, err := h.store.GetDiagnosticJob(context.Background(), envelope.Job.ID)
   		if err != nil {
  @@ -317,6 +320,74 @@ func TestAdminDiagnosticsExportAnonymizesBusinessLogs(t *testing.T) {
  +func TestDiagnosticsExportCapsAppendHeavyTablesToRecentRows(t *testing.T) {
  +	h := newHarness(t, func(w http.ResponseWriter, r *http.Request) {})
  +	ctx := context.Background()
  +	var beforeRows, beforeMaxID int64
  +	if err := h.store.ReadDB().QueryRowContext(ctx, `SELECT COUNT(*),COALESCE(MAX(id),0) FROM audit_log`).Scan(&beforeRows, &beforeMaxID); err != nil {
  +		t.Fatal(err)
  +	}
  +	const extraRows int64 = 7
  +	insertRows := diagnosticExportRowLimit + extraRows
  +	if _, err := h.store.DB().ExecContext(ctx, `
  +WITH RECURSIVE rows(value) AS (
  +	VALUES(1) UNION ALL SELECT value+1 FROM rows WHERE value<?
  +)
  +INSERT INTO audit_log(account_id,account_label,action,state,reason,detail,created_at)
  +SELECT 'historical-account-'||(value%10),'','request','alive','ok','row='||value,?
  +FROM rows`, insertRows, storage.Now()); err != nil {
  +		t.Fatal(err)
  +	}
  +
  +	raw := awaitLegacyDiagnosticExport(t, h)
  +	files := readZipFiles(t, raw)
  +	var manifest struct {
  +		Rows       map[string]int64 `json:"row_counts"`
  +		SourceRows map[string]int64 `json:"source_row_counts"`
  +		Truncated  map[string]struct {
  +			SourceRows   int64  `json:"source_rows"`
  +			ExportedRows int64  `json:"exported_rows"`
  +			OmittedRows  int64  `json:"omitted_rows"`
  +			Selection    string `json:"selection"`
  +		} `json:"truncated_tables"`
  +		RowLimit int64 `json:"large_table_row_limit"`
  +	}
  +	if err := json.Unmarshal([]byte(files["manifest.json"]), &manifest); err != nil {
  +		t.Fatal(err)
  +	}
  +	wantSource := beforeRows + insertRows
  +	if manifest.RowLimit != diagnosticExportRowLimit ||
  +		manifest.Rows["audit_log.csv"] != diagnosticExportRowLimit ||
  +		manifest.SourceRows["audit_log.csv"] != wantSource {
  +		t.Fatalf("manifest cap metadata=%+v", manifest)
  +	}
  +	truncated := manifest.Truncated["audit_log.csv"]
  +	if truncated.SourceRows != wantSource ||
  +		truncated.ExportedRows != diagnosticExportRowLimit ||
  +		truncated.OmittedRows != wantSource-diagnosticExportRowLimit ||
  +		truncated.Selection != "most_recent" {
  +		t.Fatalf("audit truncation metadata=%+v", truncated)
  +	}
  +	rows, err := csv.NewReader(strings.NewReader(files["audit_log.csv"])).ReadAll()
  +	if err != nil {
  +		t.Fatal(err)
  +	}
  +	if got := int64(len(rows) - 1); got != diagnosticExportRowLimit {
  +		t.Fatalf("exported audit rows=%d want=%d", got, diagnosticExportRowLimit)
  +	}
  +	firstID, err := strconv.ParseInt(rows[1][0], 10, 64)
  +	if err != nil {
  +		t.Fatal(err)
  +	}
  +	lastID, err := strconv.ParseInt(rows[len(rows)-1][0], 10, 64)
  +	if err != nil {
  +		t.Fatal(err)
  +	}
  +	if firstID != beforeMaxID+extraRows+1 || lastID != beforeMaxID+insertRows {
  +		t.Fatalf("export did not keep newest rows: first=%d last=%d before=%d inserted=%d", firstID, lastID, beforeMaxID, insertRows)
  +	}
  +}
  +
   func TestDiagnosticSanitizerPreservesSchemaAndEnumText(t *testing.T) {
   	codebook := buildDiagnosticCodebookWithKey(
   		[]byte("stable-diagnostic-alias-key"),
  +72 -1

internal/api/middleware.go
  @@ -120,6 +120,11 @@ func (s *Server) ServeHTTP(w http.ResponseWriter, r *http.Request) {
  +	requestBytes := r.ContentLength
  +	metricRoute := s.httpMetrics.begin(r.URL.Path)
  +	defer func() {
  +		s.httpMetrics.finish(metricRoute, rec.status, requestBytes, rec.bytes, time.Since(start))
  +	}()
   	if s.rejectForStoragePressure(r) {
   		writePublicServiceUnavailable(rec)
   		return
  +5 -0

internal/api/request_metrics.go
  @@ -0,0 +1,264 @@
  +package api
  +
  +import (
  +	"fmt"
  +	"net/http"
  +	"strconv"
  +	"strings"
  +	"sync/atomic"
  +	"time"
  +)
  +
  +// HTTP request metrics deliberately use fixed arrays rather than maps. This keeps
  +// observation O(1), allocation-free, and immune to unbounded label cardinality from
  +// user-controlled paths, account IDs, models, or status codes.
  +const (
  +	httpRouteInference = iota
  +	httpRouteAdmin
  +	httpRouteAuth
  +	httpRouteUser
  +	httpRouteHealth
  +	httpRouteOther
  +	httpRouteCount
  +)
  +
  +const (
  +	httpStatus1xx = iota
  +	httpStatus2xx
  +	httpStatus3xx
  +	httpStatus4xx
  +	httpStatus5xx
  +	httpStatusOther
  +	httpStatusClassCount
  +)
  +
  +var (
  +	httpRouteNames = [...]string{
  +		"inference",
  +		"admin",
  +		"auth",
  +		"user",
  +		"health",
  +		"other",
  +	}
  +	httpStatusClassNames = [...]string{
  +		"1xx",
  +		"2xx",
  +		"3xx",
  +		"4xx",
  +		"5xx",
  +		"other",
  +	}
  +	httpLatencyUpperBounds = [...]time.Duration{
  +		time.Millisecond,
  +		5 * time.Millisecond,
  +		10 * time.Millisecond,
  +		25 * time.Millisecond,
  +		50 * time.Millisecond,
  +		100 * time.Millisecond,
  +		250 * time.Millisecond,
  +		500 * time.Millisecond,
  +		time.Second,
  +		2500 * time.Millisecond,
  +		5 * time.Second,
  +		10 * time.Second,
  +		30 * time.Second,
  +		0, // +Inf
  +	}
  +)
  +
  +type httpRouteMetrics struct {
  +	inflight      atomic.Int64
  +	requests      atomic.Uint64
  +	requestBytes  atomic.Uint64

... (more changes truncated)
  +73 -0
[full diff: rtk git diff --no-compact]

# 两份后端诊断包时序分析

## 完整性与顺序

| 顺序 | 生成时间 (UTC) | 快照 | ZIP 校验 | SHA-256 |
|---|---|---|---|---|
| 1 | 2026-07-30T21:37:13Z | `diag_34dccc946a9481c18630e6f7fd9df08c` | ok | `dc545343aceac1989ae06d1a4ca7d4c673b73306de97ff911a4138aac3c8bdb7` |
| 2 | 2026-07-30T23:45:34Z | `diag_1ba8e03b4fe68c6264b008a369d7bf24` | ok | `fd3520c5cbb379a7ae01f03d70d6ca8734d5be95360d7e526e39b95560482c5b` |

两包均通过 ZIP CRC 完整性检查，构建版本一致，因此可直接做运行时序对比。

## 核心结论

1. **P0：目标连续性存储在硬上限前形成稳定死区。** 两次快照的占用都为 `268435116` 字节，距 `268435456` 字节硬上限仅 `340` 字节；占用是否完全相同：`true`。
2. **失败持续发生而现有磁盘守护没有制造写入余量。** 较新快照相对前一快照的 7701 秒窗口内新增 `2986` 次目标持久化降级，约 `23.265` 次/分钟。
3. **路由本身在较新窗口明显恢复，但失败审计仍是高频噪声。** 新增路由结果为 `{"success": 3256, "upstream_429": 63, "upstream_5xx": 128}`；逐次路由事实已由 `route_attempts.csv` 保存，重复的公开错误审计可限频，严格 409 身份语义必须保持逐次记录。
4. **CPA 身份连续性仍需观察，不在本批次修改语义。** ambiguous 从 `108` 增至 `310`；缺少原始客户端层级字段，直接猜测性合并身份的风险高于收益。
5. **请求体存储健康。** 较新快照内存利用率 `2.349%`，spool 利用率 `0.0%`，拒绝计数 `0`。

## 指标对比

| 指标 | 较早快照 | 较新快照 | 判断 |
|---|---:|---:|---|
| 目标存储字节 | 268435116 | 268435116 | 完全钉死 |
| 硬上限余量 | 340 | 340 | 仅 340 B |
| 导出窗口内存储失败 | 5875 | 2986 | 持续高频 |
| CPA active | 1598 | 2238 | 增长 |
| CPA ambiguous | 108 | 310 | 需继续观测 |
| 路由成功 | 1169 | 4425 | 累计增长 |
| 路由 5xx | 973 | 1101 | 较新窗口下降 |
| fresh holds | 8 | 9 | 正常波动 |
| expired unsettled | 0 | 1 | 新增 1，观测项 |
| 请求体存储拒绝 | 0 | 0 | 健康 |

## 数据窗口说明

- 较早包的 `audit_log.csv` 源表 30012 行、导出 20000 行，达到 20,000 行上限。
- 较新包的审计源表与导出均为 4998 行；其计数是保留窗口统计，不应把 summary 数值下降解释为故障自行恢复。
- `codex_upstream_attempts.csv` 两包均截断为 20,000 行；趋势判断优先使用较新快照相对前一生成时间的增量窗口。

## 修复决策

- 将磁盘守护的目标连续性存储维护阈值设为硬上限以下的低水位，在写满前主动回收终态且可回收的历史；提交路径继续使用原硬上限。
- 导出硬上限、维护目标、保留余量及实际回收量，令诊断包可直接证明守护是否生效。
- 对非 409 的同类 `routing_unavailable` 审计做有界限频；`route_attempts`、HTTP 行为和严格 409 逐次审计保持不变。
- 暂不改变 CPA 匹配、计费持有或上游重试语义，避免无证据扩大变更面。

机器可读的全部计数、目标×状态交叉表及导出截断信息见 `comparison.json`。

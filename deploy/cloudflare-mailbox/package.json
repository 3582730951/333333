{
  "name": "codex-pool-cloudflare-mailbox",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
